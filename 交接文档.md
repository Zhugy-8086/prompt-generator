这是更新到 V8.9.4 的交接文档：

# AI 绘画/视频提示词生成器 · 项目交接文档

当前版本：V8.9.4  
最后更新：2026-08-24

---

## 一、项目概述

纯前端 HTML 工具，无后端依赖，浏览器直接打开即可运行。  
将小说片段/场景描述转化为结构化 AI 绘画/视频提示词，支持复制到可灵、即梦、Runway 等平台。

核心架构：
- 双轨提取引擎：标准词库匹配 + ELIZA 反射规则  
- **HC+level 数值体系（HPDC8）**：纯整数超度量比较，领域置信度投票，确定性排序**
- 伏笔识别引擎：原句相似度对比，长短期伏笔分类，主角身份档案（五维度完整档案）  
- 三阶段混合管线：伏笔引擎身份档案 → 上下文注入器 → ELIZA 定向反射 + 过渡引擎填充  
- 引擎分层开关：用户可按需启用/关闭反射、伏笔、领域投票器、各 UI 面板  
- 模块完整性自检：自动检测挂载点和文件是否缺失  
- 错误页分流：模块加载失败时可跳转检测工具或精简版  
- 多文件章节顺序检测：支持上传多个章节文件，按自然顺序依次检测并汇总结果

---

## 二、文件结构

```

prompt-tool-v8.9.html                  ← 主文件（加载顺序 + IE 跳转 + HPDC_CONFIG 预初始化）
│
├── modules/
│   ├── hpdc-core.js                   ← ★ HC+level 数值体系核心（HPDC8 类）
│   ├── domain-voter.js                ← ★ 领域投票累加器
│   ├── dictionaries.js                ← 分册化词库（含置信度向量）
│   ├── engine-config.js               ← 引擎配置管理器（含 domainVoter 开关）
│   ├── eliza-rules.js                 ← ELIZA 反射引擎索引（含 extractWithContext）
│   ├── flow-inspector.js              ← 流程检测器（支持断点、单步调试）
│   │
│   ├── eliza-engine/                  ← 反射引擎子模块（7个）
│   │   ├── synonym-groups.js
│   │   ├── utils.js
│   │   ├── pool-manager.js
│   │   ├── theme-detector.js
│   │   ├── dynamic-boost.js
│   │   ├── builtin-rules.js           ← ★ priority 已 HPDC8 化
│   │   └── context-rules.js           ← ★ priority 已 HPDC8 化
│   │
│   ├── foreshadow-engine/             ← 伏笔引擎（全部已实现）
│   │   ├── token-utils.js
│   │   ├── anomaly-scorer.js          ← ★ 异常评分已 HPDC8 化（饱和加法）
│   │   ├── noise-filter.js
│   │   ├── identity-dictionary.js
│   │   ├── species-detector.js
│   │   ├── power-level-detector.js
│   │   ├── acquisition-tracker.js
│   │   ├── identity-merger.js
│   │   ├── protagonist-detector.js
│   │   ├── protagonist-tracker.js
│   │   ├── foreshadow-pool-manager.js ← ★ 已兼容 HPDC8 异常分比较/淘汰
│   │   ├── context-bridge.js
│   │   ├── transition-engine.js
│   │   └── foreshadow-engine.js       ← ★ 焦点词加分已兼容 HPDC8
│   │
│   ├── style-presets.js               ← 风格标签/光影/运镜/场景模板
│   └── platform-adapters.js           ← 平台适配器 + 快照管理器
│
├── ui/
│   ├── module-loader.js               ← 加载进度监控（全量模块，派发 modulesReady 事件）
│   ├── styles.css                     ← 全部样式
│   ├── ui-utils.js                    ← 通用 DOM 工具
│   ├── mode-manager.js                ← 模式切换、多镜头管理
│   ├── extract-manager.js             ← 提取调度（含领域投票自动填充、伏笔面板、批量接口）
│   ├── form-bindings.js               ← 表单事件、多文件上传、自然排序
│   ├── negative-panel.js              ← 负面提示词面板
│   ├── output-panel.js                ← 输出面板、复制、历史
│   └── init.js                        ← 初始化（含 HC 配置 UI 与持久化）
│
└── tests/
├── engine-test-v8.7.html          ← 引擎验证页（分组调试，支持断点单步）
├── eliza-ie11.html                ← IE11 精简版
├── lib/
│   ├── module-checker.js
│   └── file-scanner.js
└── scenarios/                     ← 测试用例库（11个文件）

```

---

## 三、核心功能清单

### 3.1 三模式提取

| 模式 | 机制 | 说明 |
|------|------|------|
| 标准提取 | 分册化词库正则匹配 + 领域投票器 | 覆盖 10 个分册，可选领域置信度累加自动填充 |
| ELIZA 反射 | 14 类单句规则 + 句间上下文规则 + 变体池轮询 | 含细化武器规则、情绪转折、氛围等；priority 已 HPDC8 化 |
| 综合模式 | 伏笔身份档案 → 上下文注入 → 定向反射 + 过渡填充 | 三阶段管线，产出定向叙事短语 |

### 3.2 HC+level 数值体系（V8.9 新增）

- **HPDC8 类**：三种模式（HC_ONLY / LEVEL_ONLY / HYBRID）、纯整数比较/加法、构造器、序列化
- **可配置精度**：小数层数 2/4/6，通过 `window.HPDC_CONFIG` 全局配置，持久化到 localStorage
- **领域投票器**：增量投票、批量添加、前 N 领域排序（超度量比较）
- **确定性运算**：跨平台逐位一致，无浮点，无 FPU 依赖

### 3.3 伏笔识别引擎（完整）

- 分词 → 异常度评分（HPDC8 饱和加法） → 入库 → Jaccard 相似度对比 → 长短期伏笔分类
- 自动收录 + 手动添加 + 上限管理 + 重置
- 主角身份档案（五维度：姓名、种族、等级、获取物、进化链）
- 物种检测器、等级后缀字频检测器、获取物追踪器、身份合并引擎
- 主角跨窗口追踪器（长期主角/章节主角/次要人物判定）
- 引擎可用性标志 `isAvailable`，降级时 UI 提示

### 3.4 上下文注入器与过渡引擎

- 将主角档案格式化为 `[主角:xxx][种族:xxx][等级:xxx]` 注入文本头部
- 定向反射规则（等级突破、种族凝视、进化蜕变、说话人、获取物揭示）
- 过渡引擎回收屏蔽词，提取有画面暗示的过渡句，交替插入输出

### 3.5 多文件章节顺序检测

- 支持 `.txt/.md/.markdown/.text` 多选
- 智能自然排序（中文数字/阿拉伯数字混合）
- 顺序检测按钮，逐个文件分析并汇总结果

### 3.6 反射风格滑动条

- 白描（去修饰）→ 偏白描 → 均衡 → 偏文丑 → 文丑
- 拖动时实时刷新提取结果，提示文字同步变化

### 3.7 引擎配置系统

- `window.EngineConfig` 统一管理所有开关
- **新增 domainVoter 开关**（默认关闭）
- 伏笔引擎 5 个子开关（物种检测、等级检测、获取物追踪、上下文注入、过渡引擎）
- 配置持久化到 localStorage，异常时自动回退默认

### 3.8 其他功能

- 多镜头视频提示词生成（拖拽排序、批量时长、快照撤销）
- 风格标签（13 个预设）、场景模板（13 套）
- 氛围→光影自动绑定、动作→运镜自动匹配
- 平台适配输出（5 平台）
- 负面提示词（分类 + 自定义）
- 自定义反射规则（正则 + 结果 + 优先级，已支持 HPDC8 序列化）
- 历史记录、深色模式、一键复制
- 模块完整性检测、加载进度监控（全量 30+ 关键模块）、错误页分流
- 测试工具分组调试，支持断点单步执行

---

## 四、关键模块对外接口

```javascript
// HC+level 数值体系
window.HPDC
  → HPDC8, CONFIG, normalizeToHybrid, compare, add
  → HPDC8.fromNumber(), .fromConfidence(), .fromInteger(), .fromBytes(), .fromObject()
  → HPDC8.addHCOnly(), .addLeveled(), .compare(), .serialize(), .toNumber()

// 领域投票器
window.DomainVoter
  → addVote(), addVoteMap(), getTopDomains(n), getTopDomain(), reset(), getState()
window.__DomainVoterUtils
  → createVoterFromWordList(wordList, dictionaries, options)

// 分册化词库（新增置信度接口）
window.Dictionaries / window.__DictionariesDomain
  → getDomainForWord(word), getWordListForDomain(domain)
  → getDomainConfidence(word), getTopDomain(word), mergeDomainConfidences(wordList, options)
  → __HPDC_ENABLED

// 伏笔引擎
window.ForeshadowEngine
  → scan, getPool, addManual, resetPool, setMaxItems, setEnabled, getConfig, getTotalChars, isAvailable

// 新增模块
window.__ForeshadowSpeciesDetector     → scan, detectIdentityDeclarations, detectEvolutionTargets
window.__ForeshadowPowerLevelDetector  → scan, getSuffixFrequency, splitMainSubLevel, detectPowerSystem
window.__ForeshadowAcquisitionTracker → scan, getStatsByActor
window.__ForeshadowIdentityMerger     → merge
window.__ForeshadowContextBridge      → injectContext, extractContextForReflect
window.__ForeshadowTransitionEngine   → collectFilteredWords, locateGaps, extractTransitionText, filterByVisualHint, insertIntoOutput

// ELIZA 定向反射
window.ElizaRules
  → extract, refreshExtract, extractWithContext(text, protagonistContext)
  → __LEGACY_PRIORITY（降级标志）

// 批量处理
window.ExtractManager.extractFromContent(text, fileName, callback)
window.FormBindings.handleFileUpload(event)
window.FormBindings.startBatchProcess()
```

---

五、项目惯例（保持不变）

· 不使用 ES Module，保持 file:// 兼容

· 不依赖 CDN 或 npm 包

· 所有模块通过 window.xxx 暴露

· 中文关键词用 [\u4e00-\u9fff] 正则匹配

· 提示词用中文逗号（，）分隔字段

· 禁止使用“电影感、4K、8K”等画质堆砌词

· 数据与逻辑分离

· 正则避免 g 标志（防止 lastIndex 污染）

· IIFE 中 const 变量不可重新赋值

· 测试用例按类别拆分为独立文件

---

六、版本演进路线图

```

V8.7 反射变体 + 基础上下文 + 省略号/乱码规则 + 伏笔识别
↓
V8.7.7 架构修复与工具链升级
↓
V8.8 主角身份档案 + 三阶段混合管线
↓
V8.8.1 多文件批量处理 + 自然排序 + 滑条修复
↓
V8.8.2 稳定性修复与体验优化
↓
V8.9  HC+level 数值体系集成（HPDC8 + 领域投票器 + 智能字段填充）
↓
V8.9.1 数据源层 priority HPDC8 化（builtin-rules + context-rules）
↓
V8.9.2 伏笔引擎异常分兼容 HPDC8（pool-manager + foreshadow-engine）
↓
V8.9.3 稳定性修复与检测工具完善
↓
V8.9.4 判定与分类逻辑修复（人名/种族/等级归属 + 长文场景缺陷）
↓
V9.0 分词器 + 语义雷达 + 语言模型 + 后端服务（架构升级）

```

---

## 七、V8.9.4 判定与分类逻辑修复摘要

本节记录 V8.9.3 稳定基线之后、本次针对"判断/分类准确性"所做的专项修复。全部修改向后兼容、无破坏性变更，回归测试 79/79 通过（dbg8 + dbg7，含 3 次冷/热运行）。

### 7.1 主角人名提取重写（protagonist-detector.js）
- 全局 `NAME_RE`（姓氏 + 1~2 汉字）+ `GIVEN_STOP` 废弃词表 + `maskCompoundTerms`（屏蔽"金刚不"等复合词）+ `precededByCategory`/`CATEGORY_WORDS`（屏蔽"轩辕剑"等武器前缀前的姓氏）+ 堆收集。
- 产出零误报：`[林惊羽, 苏茹, 李沧海, 张三]`，不再混入"金刚不/轩辕剑"等。
- 复姓第二字防护（`TWO_CHAR_SURNAME`）："司马"不再误拆出"马青衫"。
- 合并级前缀消歧：跨源碎片（"司马青"⊂"司马青衫"、"林惊"⊂"林惊羽"）合并时消歧。

### 7.2 种族/等级误归属修复（identity-merger.js + species-detector.js）
- `species-detector.scan` 候选人新增 `position` 字段。
- `identity-merger` 新增 `clauseIndexOf` + 从句感知合并：物种/等级只在"同句"或"40 字邻域"内归属到主角，杜绝跨句张冠李戴。
- 结果：张三→龙族、李沧海→练气期、林惊羽→荒古圣体（无种族）、苏茹→金刚不坏，均正确。

### 7.3 信息模块生成器接入主角检测器（protagonist-detector.js）
- 新增 `generateNameCandidates`/`isSurnamePrefix`，复用 `noiseFilter`、`SURNAME`、堆拒绝，与词频池合并为共享打分。
- 内置前缀消歧（生成器内 + 合并级），消除"林惊""司马青"等子串碎片。

### 7.4 贪心多取防护（protagonist-detector.js）
- `trimOverGrab`：重叠回退（"李四默"←"默默" → "李四"）。
- `NAME_DENY`：拒绝"张望/张罗/陈说/王道/李代/王法"等动词碰撞。

### 7.5 长文（整章小说）场景缺陷修复
- **异常度 NaN%**：`getPool()` 把 HPDC8 异常分对象序列化成了普通对象，丢失数值与方法。新增 `HPDC8.toConfidence()`，`getPool()` 序列化时转为 `[0,1]`；显示层兼容 number / HPDC8。现已正确显示（如 45%）。
- **"雾中气他蹲"乱码**：改为 雨/雪/风/雾 四类固定意境标签（雨幕下的朦胧 / 雪原上的寂寥 / 风中的呼啸 / 雾霭中的迷蒙）。
- **"握紧口袋里那张纸的隐藏"硬编码**：收紧为需实际"握紧/攥紧+那张纸/纸条"动作，并拆出独立的"藏不住的线索"规则。
- **"【长期伏笔】完全相同"×10 刷屏**：根因为同一文本被重复 scan 而未重置池/计数。新增防护：`checkAndCompare` 中 `distance > text.length` 即跳过，重扫现产出 0 条。
- **"号和线条"截断**：过度修饰规则中间距收紧为可选连词 `(?:的|地|得|之)?`，现正确提取"符号和线条"。

### 7.6 受影响文件
- `protagonist-detector.js`（重写+修复）、`identity-merger.js`（修复）、`species-detector.js`（修改）、`foreshadow-pool-manager.js`（修复）、`hpdc-core.js`（新增 `toConfidence`）、`eliza-engine/builtin-rules.js`（修复）、`eliza-engine/context-rules.js`（修复）、`extract-manager.js`（修复）、`tests/eliza-ie11.html`（修复）。

---

交接文档基于 V8.9.4 生成，涵盖全部已修复问题和新增能力。