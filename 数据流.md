# 数据流框架 · 结构化提示词生成器（V8.9.4）

> 本文档描述系统内部的数据流转：从"用户输入文本"到"输出结构化提示词"之间，数据经过哪些模块、以何种顺序、传递什么结构。
> 本文为**框架骨架**，章节标题与数据流节点已列全，细节（函数签名、字段含义）逐版补全。
> 贯穿全文的底层数值体系为 **HPDC8（HC+level 离散坐标，纯整数超度量比较）**，详见 §7。

---

## 1. 宏观数据流总览

```
┌──────────────────────────────────────────────────────────────────────┐
│  输入层                                                               │
│   referenceText（粘贴/上传）  ·  FormBindings（多文件批量 + 自然排序）  │
└───────────────────────────────┬──────────────────────────────────────┘
                                 │  ExtractManager.performExtract()
                                 ▼
┌──────────────────────────────────────────────────────────────────────┐
│  模式分派（EngineConfig 开关）                                         │
│   标准提取  │  反射提取  │  综合模式（三阶段管线，见 §6）               │
└───────────────────────────────┬──────────────────────────────────────┘
                                 │
        ┌────────────────────────┼────────────────────────┐
        ▼                        ▼                        ▼
 ┌─────────────┐         ┌──────────────┐        ┌──────────────────┐
 │ 标准提取    │         │ ELIZA 反射   │        │ 伏笔引擎         │
 │ Dictionaries│         │ builtin+      │        │ 分词→异常评分→   │
 │ +DomainVoter│         │ context-rules │        │ 入库→相似度对比→ │
 │             │         │ +变体池+风格  │        │ 主角五维档案     │
 └──────┬──────┘         └──────┬───────┘        └────────┬─────────┘
        │                       │                        │
        └───────────────────────┼────────────────────────┘
                                 ▼
                 ┌───────────────────────────────┐
                 │ 过渡引擎（回收屏蔽词→填补空缺） │  ← 仅综合模式
                 └──────────────────┬────────────┘
                                     ▼
                 ┌───────────────────────────────┐
                 │ 输出装配（ExtractManager）     │
                 │ → output-panel / negative-panel │
                 │ → platform-adapters（5 平台）   │
                 └───────────────────────────────┘
```

---

## 2. 模块加载与初始化流

```
prompt-tool-v8.9.html
  ├─ <head> 内联：HPDC_CONFIG 预初始化（defer 前的兜底）
  ├─ 脚本加载顺序（defer 并行）：
  │   1. hpdc-core.js        → window.HPDC
  │   2. dictionaries.js     → window.Dictionaries / __DictionariesDomain
  │   3. domain-voter.js     → window.DomainVoter
  │   4. engine-config.js    → window.EngineConfig
  │   5. eliza-engine/*      → window.ElizaRules / __Eliza*
  │   6. foreshadow-engine/* → window.ForeshadowEngine / __Foreshadow*
  │   7. style-presets.js / platform-adapters.js
  │   8. ui/*                → module-loader / init / extract-manager ...
  └─ module-loader.js：监控 30+ 关键挂载点
        └─ 全量就绪 → 派发 modulesReady 事件
              └─ init.js 监听 modulesReady → 初始化 EngineConfig / HPDC 设置 UI
```

**关键数据/状态**：
- `window.HPDC_CONFIG`（localStorage，frac 层数 / 模式 / 饱和开关）
- `window.EngineConfig`（引擎级 + 面板级 + 伏笔子开关，localStorage）
- 模块缺失/降级：`isAvailable` 标志、错误页分流（检测工具 / 精简版）

---

## 3. 标准提取数据流

```
输入 text
  └─ Dictionaries 分册词库匹配（正则 + 词边界断言）
        └─ 命中词 → getDomainForWord / getDomainConfidence(word) → {领域: HPDC8}
              └─ DomainVoter.addVoteMap(wordList)  →  增量投票累加（HPDC8 饱和加法）
                    └─ getTopDomains(2)  →  前 N 领域
                          └─ DOMAIN_FIELD_MAP  →  scene/subject/mood/camera 字段填充
                                └─ fillFieldsFromTopDomains()  →  输出字段
```

- 开关：`EngineConfig.isEnabled('domainVoter')`（默认关）；未加载 HPDC → `legacyFillFromKeywords()` 降级。
- 输出：结构化字段（媒体类型/画幅/场景/主体/动作/情绪/氛围/运镜…）。

---

## 4. ELIZA 反射数据流

```
输入 text（或 text + protagonistContext）
  └─ ElizaRules.extract(text)  /  extractWithContext(text, ctx)
        ├─ builtin-rules（14 类单句规则）
        ├─ context-rules（句间因果/时序、省略号无语、乱码主题、伏笔前置信号）
        ├─ 每条规则带 priority: HPDC8  →  排序用 HPDC8.compare(b.priority, a.priority)
        ├─ 变体池 pickFromPool()（高优规则 3~6 变体，轮询去重）
        ├─ 动态加成 boost.*Boost 经 HPDC8.addLeveled 合并
        ├─ 同义词去重（同组只留最优）
        ├─ applyReflectStyle()（白描↔文丑 后处理，按滑条区间裁剪修饰）
        └─ 输出：金色叙事标签数组（反射结果）
```

- 定向反射（综合模式）：`extractWithContext` 注入 `protagonistProfile`，触发 5 条定向规则（等级突破/种族凝视/进化蜕变/说话人/获取物揭示）。
- 降级：`__LEGACY_PRIORITY` 标志，无 HPDC → number 优先级。

---

## 5. 伏笔引擎数据流

```
ForeshadowEngine.scan(text, {mood})
  │
  ├─ 1. token-utils：分词 / 句子切分 / 氛围检测
  │
  ├─ 2. 异常评分（HPDC8）
  │     for each candidate word:
  │       calculateAnomalyScore(word, text, pos)
  │         → { score: HPDC8(HC_ONLY 饱和加和), reasons[] }
  │           加分项：未解释 / 过度修饰 / 引号内 / 视角聚焦 / 条件暗示 / 信息差 / 明示
  │       shouldEnterPool() → HPDC8.compare(score, THRESHOLD_ENTER) >= 0 ?
  │
  ├─ 3. 入库（foreshadow-pool-manager）
  │     addToPool(word, sentence, pos, mood, quoted, anomalyScore(HPDC8), source, maxItems)
  │       → 存储 entry{anomalyScore: HPDC8, occurrences, firstPosition, ...}
  │       → 超限：anomalyLessThan 比较踢出最低分项
  │
  ├─ 4. 相似度对比（checkAndCompare）
  │     Jaccard(sentence, entry.firstSentence)
  │       ≥ 高相似 + 距离>3000字 → 长期伏笔（复读机式）
  │       中相似 → 疑似伏笔
  │       防御：distance > text.length → 跨扫描伪匹配，跳过（见 §5.5）
  │
  └─ 5. 主角身份档案（5 维）
        protagonist-detector.detectIdentityProfiles(text)
          ├─ NAME_RE（姓氏+1~2汉字）+ GIVEN_STOP + maskCompoundTerms
          │   + precededByCategory/CATEGORY_WORDS + 堆收集  → 人名候选
          ├─ species-detector.scan（候选人带 position）
          ├─ power-level-detector.scan
          ├─ acquisition-tracker.scan
          └─ identity-merger.merge（clauseIndexOf + 同句/40字邻域感知）
                → protagonistProfile[]{ 姓名, 种族, 等级, 获取物, 进化链 }
        protagonist-tracker.recordScanProfiles()  →  跨扫描累积
```

### 5.1 核心数据结构（草）
```
HPDC8 异常分对象：{ sign, level, int_part, frac[] }  →  toConfidence()→[0,1]
PoolEntry：{ word, sentence, position, mood, quoted,
             anomalyScore: HPDC8, source, occurrences[], firstPosition }
ProtagonistProfile：{ name, race, level, acquisitions[], evolutionChain[], chainLength }
```

### 5.2 主角人名提取（重写后）
- 全局 `NAME_RE`（姓氏 + 1~2 汉字）+ `GIVEN_STOP` 废弃词表 + `maskCompoundTerms`（屏蔽"金刚不"等）+ `precededByCategory`（屏蔽"轩辕剑"等武器前缀）+ 堆收集。
- 复姓第二字防护 `TWO_CHAR_SURNAME`；合并级前缀消歧（消除"林惊""司马青"等子串碎片）。
- 产出零误报：`[林惊羽, 苏茹, 李沧海, 张三]`。

### 5.3 种族/等级归属（从句感知）
- `species-detector` 候选人带 `position`；`identity-merger` 用 `clauseIndexOf` 仅在同句或 40 字邻域内归属，杜绝跨句张冠李戴。
- 示例：张三→龙族、李沧海→练气期、林惊羽→荒古圣体（无种族）、苏茹→金刚不坏。

### 5.4 贪心多取防护
- `trimOverGrab`（重叠回退："李四默"←"默默"→"李四"）。
- `NAME_DENY`（拒绝"张望/张罗/陈说/王道/李代/王法"等动词碰撞）。

### 5.5 长文（整章）场景注意点
- `getPool()` 序列化时 HPDC8 → `[0,1]`（否则显示 NaN%）。
- 重复 scan 同一文本：距离防护跳过自匹配，避免"长期伏笔 ×N"刷屏。
- 过度修饰中间距收紧为连词 `(?:的|地|得|之)?`，避免"符号和线条"被截成"号和线条"。

---

## 6. 综合模式 · 三阶段管线

```
阶段1：ForeshadowEngine.scan(text)  →  protagonistProfile + 伏笔池
                     │
阶段2：contextBridge.injectContext(profile)  →  文本头部注入 [主角:][种族:][等级:]
                     + ElizaRules.extractWithContext(text, ctx)  →  定向叙事短语
                     │
阶段3：transitionEngine
          collectFilteredWords()      （回收前两阶段屏蔽词）
          locateGaps()                （定位核心短语间空缺，需 position 字段）
          extractTransitionText()     （反向提取原文本空缺区过渡句）
          filterByVisualHint()        （保留具象名词+动作，丢弃纯功能过渡）
          insertIntoOutput()          （核心→过渡→核心 交替插入）
                     │
                     ▼
             输出结构（定向叙事 + 过渡填充）
```

- 降级：伏笔/反射任一未开启 → 自动降级到可用模式；过渡引擎关闭 → 一二阶段照常。

---

## 7. HPDC8 数值主干（横切关注点）

```
所有"比较/排序/累加"统一走 HPDC8，消除浮点不确定：

  ├─ 领域投票    DomainVoter        addHCOnly / 饱和加法
  ├─ ELIZA 优先级 builtin/context   priority: HPDC8，compare 排序
  ├─ 异常评分    anomaly-scorer     calculateAnomalyScore → HPDC8(HC_ONLY)
  ├─ 伏笔池      pool-manager       anomalyLessThan / 淘汰 / focus-word boost
  └─ 配置        hpdc-core          fromConfidence/fromInteger/serialize/toConfidence

  getPool() 序列化：HPDC8 → toConfidence() 转 [0,1] 数值，供显示层消费
```

- 确定性：纯整数运算，跨平台逐位一致；`frac` 层数编译期固定，改配置需刷新。
- 降级：无 `window.HPDC` → 各模块回退到 number / legacy 函数。

---

## 8. 状态与持久化

```
内存态（每次 scan / 每文件重置）：
  ├─ foreshadow-pool（伏笔词库，不跨文件累积；批量处理每文件重置）
  ├─ protagonist-tracker.cumulativeProfiles（跨扫描累积维度历史）
  └─ ELIZA 变体池使用记录（会话级，刷新重置）

持久化（localStorage）：
  ├─ HPDC_CONFIG（frac 层数 / 模式 / 饱和）
  ├─ EngineConfig（引擎/面板/伏笔子开关，deepMerge 兼容旧配置）
  ├─ 用户自定义反射规则（priority 序列化存 HPDC8 或 number 兼容）
  └─ 历史记录（最近 N 条生成结果）
```

---

## 9. 关键模块对外接口（索引）

```
window.HPDC          → HPDC8, CONFIG, compare, add, fromConfidence, toConfidence ...
window.DomainVoter   → addVote, addVoteMap, getTopDomains, getTopDomain, reset
window.Dictionaries  → getDomainForWord, getDomainConfidence, getTopDomain, mergeDomainConfidences
window.ForeshadowEngine → scan, getPool, addManual, resetPool, setMaxItems, isAvailable
window.__Foreshadow* → speciesDetector / powerLevelDetector / acquisitionTracker
                         / identityMerger / contextBridge / transitionEngine
window.ElizaRules    → extract, refreshExtract, extractWithContext, __LEGACY_PRIORITY
window.ExtractManager→ performExtract, extractFromContent(text, fileName, cb)
```

---

## 10. 待补全 / 开放问题（框架占位）

- [ ] §3/§4/§5 各步骤补全精确函数签名与字段类型
- [ ] §5.1 数据结构补全完整字段（occurrences 结构、evolutionChain 格式）
- [x] 批量多文件：词库重置时序图（见 §11.2）
- [ ] 综合模式输出结构 schema（核心/过渡交替的数组形态）
- [x] 错误页分流与降级路径的时序图（见 §11.3）
- [ ] 与 v0.03 大纲排期（V9 分词器 / V10 辩证反射 / V11 情感语境 / V12 动作链）的对齐标注

---

## 11. 时序图（详细调用链）

> 本节用 ASCII 时序图补充 §6 三阶段管线与批量处理的真实调用顺序。
> 顺序均依据 `ui/extract-manager.js`、`ui/form-bindings.js` 实际代码核对（V8.9.4）。

### 11.1 单条文本 · 综合模式一次性调用链

参与者：User → ExtractManager.performExtract → ProtagonistDetector → ElizaRules → TransitionEngine → ForeshadowEngine → Panels

```
 User
   │ 点击「综合」提取
   ▼
 ExtractManager.performExtract(text)
   │ 1. standardExtract(text) ───────────────────► words[]
   │ 2. (domainVoter 开) DomainVoter 累加 → 字段自动填充
   │
   │ 3. 进入 composite 分支：
   │    3a. detectIdentityProfiles(text) ────────► ProtagonistDetector
   │        │                                      ├─ NAME_RE + GIVEN_STOP
   │        │                                      ├─ maskCompoundTerms / precededByCategory
   │        │                                      ├─ species/power/acquisition 合并
   │        │                                      └─ identity-merger(clauseIndexOf, 40字邻域)
   │        │←──── profiles[]  (五维: 姓名/种族/等级/获取物/进化链)
   │    3b. 取 profiles[0] 组装 protagonistContext
   │    3c. extractWithContext(text, ctx) ───────► ElizaRules
   │        │                                      ├─ builtin + context 规则
   │        │                                      ├─ priority: HPDC8 排序
   │        │                                      ├─ 变体池 pickFromPool
   │        │                                      └─ 5 条定向规则(等级突破/种族凝视/进化/说话人/获取物)
   │        │←──── elizaRes{ items[], directed[] }
   │    3d. applyReflectStyle(items / directed)
   │    3e. 过渡引擎：
   │        │ split 句子 → usedSet = eliza+standard → unused = 其余
   │        │ filterByVisualHint(unused) ─────────► TransitionEngine → transitionPhrases[]
   │        │ coreItems = directed+items(≤20) → outputStructure{text,position}
   │        │ insertIntoOutput(outputStructure, transitionPhrases) ─► 核心↔过渡 交替
   │        ▼
   │    3f. allTags = outputStructure 映射(core/transition/directed)
   │
   │ 4. (foreshadow 开) ForeshadowEngine.scan(text,{mood}) ─► ForeshadowEngine
   │        ├─ token-utils 分词/分句/氛围
   │        ├─ anomalyScorer.calculateAnomalyScore → HPDC8(HC 饱和加和)
   │        ├─ poolManager.addToPool(HPDC8)  (超上限按 HPDC8 淘汰)
   │        ├─ checkAndCompare → 长期/短期伏笔(distance>text.length 跳过重扫)
   │        └─ 内部再 detectIdentityProfiles → 主角档案(与 3a 重复计算)
   │        │←──── foreshadowResult[](pool + 信号 + profiles)
   │
   │ 5. renderTags(allTags) / renderForeshadowPanel / renderProtagonistPanel
   ▼
 User ← 输出（定向叙事 + 过渡填充 + 伏笔标签）
```

> 注：代码中 `scan` 在 composite 的 ELIZA/过渡之后才调用（步骤 4），且 3a 与 4 内部各算一次主角档案；逻辑上仍可理解为「身份档案 → 定向反射+过渡 → 伏笔面板」三阶段。上下文通过 `extractWithContext(text, ctx)` 直接传入，未单独调用 `contextBridge.injectContext`。

### 11.1.1 detectIdentityProfiles 内部子流程（四路检测 → 合并）

对应 §11.1 步骤 3a。四路检测彼此独立，最后由 `identity-merger` 合并；`clauseIndexOf` 保证物种/等级只在「同句 / 40 字邻域」内归属到人名，避免跨句张冠李戴。

```
 detectIdentityProfiles(text)
   │
   ├─ 1. detectAll(text) ─────────────────────► 人名候选[]
   │        │  (NAME_RE 姓氏+1~2字 + GIVEN_STOP + maskCompoundTerms
   │        │   + precededByCategory 武器前缀 + 堆收集 + 复姓/前缀消歧)
   │        │←── humanCandidates[]{ name, score, position }
   │
   ├─ 2. speciesDetector.scan(text) ──────────► SpeciesDetector
   │        │  (词典 120 条 + 正则骨架；带 position；
   │        │   进化/命名/系统面板加权；复合词保护)
   │        │←── speciesCandidates[]{ label, confidence, position }
   │
   ├─ 3. powerDetector.scan(text) ─────────────► PowerLevelDetector
   │        │  (后缀字频发现等级体系；主/子等级拆分)
   │        │←── powerLevels{ mainLevels[], subLevels[], ... }
   │
   ├─ 4. acquisitionTracker.scan(text, allCandidates)
   │        │      allCandidates = humanCandidates
   │        │          + speciesCandidates(映射为 {name,score,freq})
   │        └─► AcquisitionTracker
   │               (触发动词 → 三层归属推断 → 物品权重 S~D 级)
   │             getStatsByActor() ─────────────► acquisitionStats{ actor: 权重 }
   │
   ├─ 5. speciesDetector.detectEvolutionChain(text) ─► 进化链[]
   │        │  (前缀/后缀重叠度 >0.5 合并为链)
   │        │←── evolutionChains[]{ labels[], length }
   │
   └─ 6. merger.merge(humanCandidates, speciesCandidates,
                       powerLevels, acquisitionStats,
                       evolutionChains, text) ──────► IdentityMerger
           │  for 每个主实体(humanCandidate):
           │    ├─ clauseIndexOf：在「同句 / 40 字邻域」内匹配 species/power
           │    ├─ 填充五维：姓名 / 种族 / 等级 / 获取物 / 进化链
           │    └─ 综合置信度 = 人名×2 + 物种 + 获取物×0.1 + 进化链 + 等级
           │←── profiles[]{ 五维档案 }
   │
   └─ (无 merger) 降级：手工构建简易档案 fallbackProfile
```

> 注：四路检测在代码中为顺序执行（非真正并行），但互不依赖、可独立失败（各带 try/catch，单路异常不影响其余）。`acquisitionTracker.scan` 需要人名+物种候选池做归属推断，故第 4 步把 humanCandidates 与 speciesCandidates 合并为 `allCandidates` 传入。合并是唯一把四路结果汇总的环节；`clauseIndexOf` 的「同句/40 字邻域」约束正是 V8.9.4 修复跨句误归属（张三→龙族 等）的关键。

### 11.2 批量多文件 · 词库重置时序

参与者：User → FormBindings.startBatchProcess → FileReader → ExtractManager.extractFromContent → performExtract → ForeshadowEngine → FlowInspector(字数漂移)

```
 User
   │ 选择多个 .txt/.md → handleFileUpload → naturalSortFiles() 按章节序
   ▼
 FormBindings.startBatchProcess()
   │ for i in 0..N-1:
   │   │
   │   ├─ readFileContent(file) ──► FileReader.readAsText ──► content (Promise)
   │   ├─ referenceText.value = content        （仅 UI 预览；提取用传入的 content）
   │   ├─ await extractFromContent(content, name, cb)
   │   │      │
   │   │      └─► performExtract(content, …)
   │   │             ├─ standardExtract + (domainVoter?)
   │   │             ├─ composite 分支（detectIdentityProfiles → extractWithContext → transition）
   │   │             ├─ ForeshadowEngine.scan(content)  ──► 累计写入 pool + fsTotalCharsRead
   │   │             └─ renderTags / 面板
   │   │      └─ cb(tags) → batchResultDiv 追加本文件摘要
   │   │
   │   ├─ progressBar = (i+1)/N × 100%
   │   └─ ⚠️ 循环体内**无显式 resetPool**
   ▼
 ── 伏笔池跨文件累积，依赖三道防护避免污染 ──
   (A) FlowInspector「字数漂移」步骤（每次 performExtract 后执行）：
         if (fsTotalCharsRead > textLength × 3)  →  ForeshadowEngine.resetPool()   ← 自动重置
   (B) checkAndCompare 的 distance 防护：
         if (distance > text.length)  跳过（重扫自匹配不告警，见 §5.5）
   (C) 手动重置：resetForeshadowPool()（confirm 弹窗，仅用户点击）

 全部文件处理完 → batchResultDiv 追加「✅ 批量处理完成」；batchInProgress = false
```

> 注：V8.8.1 变更记录称「词库在每次 scan 后重置」，实际代码未在批量循环里显式 `resetPool`，跨文件隔离主要由 (A) 字数漂移守卫 + (B) 重扫 distance 守卫实现；(C) 为人工触发。若需严格每文件清零，应在 `startBatchProcess` 每轮开头调用 `ForeshadowEngine.resetPool()`。

---

## 11.3 错误页分流与降级路径时序

> 依据 `ui/module-loader.js`、`ui/init.js`、`prompt-tool-v8.9.html`、`tests/engine-test-v8.7.html`、`tests/eliza-ie11.html` 实际代码核对（V8.9.4）。
> 分两层：**加载期分流**（入口 → 检测工具 / 精简版）与 **运行期降级**（功能缺失时各引擎各自退避）。

### 11.3.1 加载失败 → 错误页分流时序

参与者：Browser/入口 → module-loader → ErrorPage → User → engine-test(检测工具) ↔ eliza-ie11(精简版)

```
 Browser / prompt-tool-v8.9.html
   │ 并行加载脚本（defer）
   ▼
 module-loader.js（监控 30+ 关键挂载点）
   │ ├─ 全部就绪 → window.modulesReady = true → 派发 modulesReady 事件
   │ │     └─► init.js 监听 modulesReady（IE11 下降级为轮询检测）
   │ │           └─► AppSkeleton.render() 渲染界面
   │ └─ 超时/缺失 → 渲染错误页（loading-fallback 区替换为分流 UI）
   ▼
 ErrorPage（分流 UI，含两个按钮）
   │  [进入检测工具] → tests/engine-test-v8.7.html?from=error&missing=XXX
   │  [进入精简版]   → tests/eliza-ie11.html?mode=fallback
   ▼  User 点击
   ├───────────────────────────┐
   ▼                           ▼
 engine-test-v8.7.html        eliza-ie11.html（精简版）
   │ 加载 module-checker.js      │ ?mode=fallback → 显示
   │     + file-scanner.js          fallbackNotice 降级提示
   │ module-checker：对比预期      │ ?auto=ie → IE 自动跳转标记
   │   挂载点，缺失/新增红绿横幅   │ 独立、ES5、不引入 HPDC 依赖
   │ file-scanner：扫描文件完整性
   │ ?from=error&missing=XXX → 自动高亮该模块
   │ ├─ 可跳转精简版
   │ └─ 给出修复指引（缺失文件名 → 用户补齐）
   ▼
 入口 ↔ 检测工具 ↔ 精简版 三者互相连通（用户不被卡死）
```

> 注：`prompt-tool-v8.9.html` 自身在检测到 IE 且当前非精简版时，会 `window.location.replace('tests/eliza-ie11.html?auto=ie')` 直接跳转，无需用户点按钮。检测工具与精简版均提供 `?from=error` / `?mode=fallback` / `?auto=ie` 参数互通。

### 11.3.2 运行期降级决策（performExtract 内 + 模块级）

参与者：ExtractManager.performExtract → 各引擎（带 isAvailable / try-catch）

```
 performExtract(text)
   │
   ├─ standardExtract(text)  ── 始终执行（不依赖 HPDC）
   │
   ├─ 领域投票填充？
   │     if (EC.isEnabled('domainVoter') && window.DomainVoter && window.HPDC)
   │         → DomainVoter 累加 + fillFieldsFromTopDomains
   │     else → legacyFillFromKeywords(words)              ← 降级
   │
   ├─ 模式分派（EngineConfig 开关）：
   │     composite ? 需 eliza && foreshadow 同时开
   │         └─ 缺其一 → 降级到 eliza / standard
   │     eliza ? 需 ElizaRules；若 window.HPDC 缺失
   │         → __LEGACY_PRIORITY（number 优先级），extract 仍可用  ← 降级
   │
   ├─ 伏笔扫描？
   │     if (window.ForeshadowEngine && EC.isEnabled('foreshadow'))
   │         → scan(text)（内部 anomaly-scorer：无 HPDC → legacy 函数）
   │     else → 跳过扫描；伏笔面板不累积；UI 显示「关」      ← 降级
   │           （ForeshadowEngine.isAvailable 标志驱动 UI 提示）
   │
   └─ 单模块异常：各调用 try/catch 包裹
         speciesDetector 异常 → 仅缺「种族」维度，姓名/等级不受影响
         acquisitionTracker 异常 → 仅缺「获取物」，其余照常
         （detectIdentityProfiles 内每路独立 try/catch，见 §11.1.1）
```

> 注：HPDC 缺失会触发**级联降级**（V8.9 报告·四）：`dictionaries` 新接口抛错但旧接口正常；`eliza-rules` 自动降级到 number 优先级；`anomaly-scorer` 用 legacy 函数；`domainVoter` 跳过。旧 localStorage 规则 `priority` 为数字时，`eliza-rules` 自动转 HPDC8 并警告一次。所有降级均向后兼容、无破坏性；`isAvailable` 标志（如 `ForeshadowEngine.isAvailable`）供 UI 明确感知可用性。

### 11.3.3 ForeshadowEngine.scan 内部子流程（异常评分 → 入库 → 相似度对比）

对应 §11.1 步骤 4、§11.3.2 伏笔扫描分支。逐个候选词走「评分 → 入库 → 对比」三步，HPDC8 贯穿始终。

```
 ForeshadowEngine.scan(text, {mood})
   │ 0. 字符状态同步：
   │      if (text !== lastScannedText) totalCharsRead = 0;
   │      totalCharsRead += text.length;
   │      absolutePosition = totalCharsRead - text.length + wordPosition
   │        （单次扫描时 ≈ wordPosition；重扫时带历史偏移）
   │ 1. 主角：protagonistDetector.detect(text) + tracker（长期主角覆盖）
   │ 2. tokenize → uniqueWords[]
   │
   ▼ for each word (len≥2), for each 出现位置 wordPosition:
   ├─ 3. shouldEnterPool(word, text, wordPosition) ─────► AnomalyScorer
   │        │  noiseFilter.isNoiseWord(word) ? → enter=false（直接淘汰）
   │        │  else calculateAnomalyScore → { score: HPDC8, reasons[] }
   │        │     score 从 fromConfidence(0) 起，HPDC8.addHCOnly 累加：
   │        │       isQuoted          → +BOOST_QUOTED
   │        │       isOverModified    → +BOOST_OVERMODIFIED
   │        │       isPreciseTime/Num → +BOOST_PRECISE
   │        │       isPerspectiveFocus→ +BOOST_PERSPECTIVE
   │        │       isConditionalHint → +BOOST_CONDITIONAL
   │        │       !hasExplanation   → +BOOST_UNEXPLAINED
   │        │  enter = HPDC8.compare(score, THRESHOLD_ENTER(0.45)) >= 0
   │        │←── { enter, anomaly:{ score: HPDC8, reasons } }
   │        │
   │        ├─ enter? → addToPool(word, sentence, absolutePosition, mood,
   │        │             quoted, score(HPDC8), 'auto', maxItems)
   │        │              └─► PoolManager.addToPool
   │        │                    ├─ 同词已存在 → 跳过（去重）
   │        │                    ├─ 池满(maxItems)：找最低分(anomalyLessThan)
   │        │                    │     新项不严格优于最低分 → 拒绝
   │        │                    │     else 踢出最低分，push entry{
   │        │                    │         anomalyScore: HPDC8, occurrences[],
   │        │                    │         firstSentence, firstPosition, ... }
   │        │                    └─ 否则直接 push
   │        │
   │        └─ 4. checkAndCompare(word, text, absolutePosition, 0.6, similarityMid)
   │                 └─► PoolManager.checkAndCompare
   │                   for each 同词 entry（非 recycled）:
   │                     currentSentence = extractSentence(text, word, position)
   │                     similarity = jaccardSimilarity(firstSentence, currentSentence)
   │                     distance   = position - entry.firstPosition
   │                     if (distance > text.length) continue;  ← V8.9.4 重扫防护
   │                     if (similarity >= similarityMid) 记录 occurrence
   │                     ├─ similarity >= 0.6(high):
   │                     │     type = distance>3000 ? 'foreshadow_long'
   │                     │                         : 'foreshadow_short'
   │                     │     msg = 【长期/短期伏笔】+ 完全相同/高度相似 + 跨 N 字
   │                     │     calm→violent 氛围 → 追加「戏剧张力升级」
   │                     └─ similarity >= similarityMid:
   │                           type='foreshadow_suspected'；【疑似伏笔】
   │                     去重键 = firstSentence | currentSentence
   │                   ←── results[]（伏笔信号）
   │
   ▼ 5. 视角聚焦加成：isPerspectiveFocus → 焦点词 shouldEnterPool
   │        → +0.1(HPDC8.addHCOnly) → addToPool(mood='伏笔信号')
   ▼ 6. 结果归一化（normSentence 去首尾标点）/ 同场景去重
   ▼ 7. return results[]（供 performExtract → 伏笔面板）
```

> 注：`scan` 内部只用 `protagonistDetector.detect()` 做追踪级主角名（喂给 `tracker`），完整的五维档案走 §11.1.1 的 `detectIdentityProfiles`。`anomalyScore` 以 HPDC8 存入池；`getPool()` 序列化时经 `toConfidence()` 转 `[0,1]`（V8.9.4 修复 NaN%）。LEGACY 模式下 `calculateAnomalyScore`/`shouldEnterPool` 走 number 版，阈值同为 0.45。


```
