# 结构化提示词生成器 · 更新日志

## V1.0 · 初始版本

**新增功能：**
- 基础表单界面：媒体类型、画幅、场景、主体、动作、情绪
- 单镜头/多镜头模式切换
- 自动生成结构化提示词（图片/视频两种格式）
- 一键复制输出结果到剪贴板
- 深色模式切换

**解决问题：**
- 解决纯文本描述导致AI理解偏差的问题
- 用结构化表单约束用户输入，减少遗漏字段

---

## V1.1 · 增强版

**新增功能：**
- 文件上传功能（支持 `.txt`，自动读取文本内容）
- 长文本参考区域（上传或粘贴小说片段作为灵感参考）
- 使用提示（黄色提示框，防止用户误解上传=自动填充）
- Word文档兼容建议（提示用户先另存为 `.txt`）

**解决问题：**
- 用户输入不一定是一个具体场景，可能是一大段小说
- 提供参考文本区辅助用户提炼关键字段

---

## V2.0 · 智能增强版

**新增功能：**
- 关键词自动提取（从参考文本中提取高频词，点击可填入对应字段）
- 风格标签系统（10个预设风格标签，点击注入氛围描述）
- 负面提示词改为可勾选清单（A/B/C/D分类，自由勾选）
- 历史记录保存（localStorage保存最近5条生成结果，支持点击加载）
- 面板折叠功能（参考文本、风格标签、负面词、历史记录四个区域可折叠）
- 清空全部按钮

**修复问题：**
- 修复关键词点击无限追加的bug（点击第二次取消填入）
- 修复关键词提取质量差的问题（正则切片碎片化）

**解决问题：**
- 手动提炼关键词成本高，增加自动提取降低门槛
- 多次使用无历史回溯，增加localStorage持久化
- 负面提示词默认全选不灵活，改为可勾选

---

## V3.0 · 多镜头专业版

**新增功能：**
- 单镜头/多镜头自由切换按钮
- 多镜头分段输入UI（每个镜头独立填写动作、运镜、时长）
- 公共字段继承机制（场景/主体/氛围可自动继承，可单独覆盖）
- 运镜模板下拉选择器（8个预设运镜模板，快速填充）
- 总时长自动累加与预警（超过各工具稳定上限时红色警告）
- 镜头删除功能

**输出改进：**
- 多镜头模式输出按 `【镜头1·X秒】【镜头2·X秒】` 格式分段

**解决问题：**
- 视频提示词中“然后”“接着”串联多个动作导致AI动作乱跳
- 每个镜头独立控制，避免多个动作糊成一团

---

## V3.2 · TextRank引擎版

**核心升级：**
- 关键词提取从简单正则切片升级为TextRank图算法
- 滑动窗口共现图（窗口大小5个词，迭代收敛取Top 15）
- 扩展停用词表（新增“东西”“皮信封”“把整条街”等碎片词过滤）

**修复问题：**
- 彻底解决提取“东西”“皮信封”“写一个小”等碎片化问题
- 提取质量大幅提升（语义相关词被正确提升，如“雨夜”+“便利店”）

---

## V3.3 · 最终版（单文件）

**核心升级：**
- 关键词提取从TextRank切换为“已知视觉实体+通用短语”混合模式
- 跨题材词表扩展（奇幻、仙侠、科幻、都市、直播、政治、抽象概念等）
- 通用中文短语提取器作为兜底（智能分句+停用词过滤+“的”字短语提取）
- 关键词提取结果按长度和质量优先排序

**解决问题：**
- TextRank面对某些文本仍产生碎片（如“像一块浸”“在水里的”）
- 词表过于局限城市/便利店场景，对奇幻仙侠异世界基本失效

---

## V4.0 · 全题材覆盖版

**核心升级：**
- 标准词表大幅扩展：新增直播/互联网/娱乐词汇、政治/抽象概念词汇
- 扩展自然/奇幻/仙侠/科幻/人物实体词条
- 通用提取停用词表持续优化

---

## V5.0 · ELIZA反射模式

**核心升级：**
- 新增“标准提取/反射提取”双模式切换
- ELIZA反射规则引擎（8大类规则，68条规则）：
  - 人物+状态：孤独的守望者、沉默的身影
  - 地点+动作：城墙上的守望者、门旁的身影
  - 物件+修饰：破碎的城墙、泛黄的信封
  - 情绪反射：悲哀的眼神、含泪的眼眸
  - 空间凝视：望向远方的凝视、回眸的一瞬
  - 氛围自然：黄昏的余晖、黎明的微光
  - 宏大叙事：硝烟弥漫的战场、飘扬的旗帜
  - 直播娱乐：滚动的弹幕、聚光灯下的舞台
- 反射结果以金色标签显示，与标准提取区分

**设计理念：**
- 借鉴ELIZA DOCTOR脚本的“反射”思想——不直接理解文本，而是通过模式匹配反射出更有叙事张力的关键词短语
- “孤独的老人”比“老人”更能表达画面
- “破碎的城墙”比“城墙”更有叙事感

---

## V5.5 · 反射扩展+光影映射（未独立发版，合入V6）

**新增：**
- 反射规则扩展第9-14类：巨大空镜、读者名场面反应、身份揭示、白描法、神性/虚无/荒诞、烟火气/市井
- 氛围—光影绑定映射表（15条情绪→光影映射）
- 动作—运镜自动匹配词典（20条动作→运镜映射）
- 平台适配器模块（可灵/即梦/Runway/PixVerse/Veo等5个平台专属模板）

**数据来源：**
- 搜索了小说社区“名场面”读者反应高频词
- 参考了毕飞宇《促织》分析、江南式“巨大化写作”、晋江段评文化

---

## V6.0 · 搜索增强版（单文件集成）

**合并内容：**
- V4的全题材词表
- V5的14类ELIZA反射规则
- V5.5的氛围光影绑定、动作运镜匹配、平台适配器
- 全部集成于单一HTML文件
- 关键词菜单化填入（点击标签弹出菜单，可选填场景/主体/氛围）
- 负面词全选/清空/反选按钮
- 多镜头批量时长设置
- 多镜头上下排序（↑↓按钮）
- 移动端响应式适配
- 复制功能强化兼容

---

## V7.0 · 模块化架构

**架构升级：**
- 主文件拆分为1个主文件+4个外部模块：
  - `prompt-tool-v7.html`：主文件（~450行，精简核心流程）
  - `modules/dictionaries.js`：静态词库（标准提取词条+停用词表）
  - `modules/eliza-rules.js`：ELIZA反射规则引擎（14类规则+同义词去重）
  - `modules/style-presets.js`：风格标签+光影映射+运镜映射+场景模板库
  - `modules/platform-adapters.js`：平台适配器（5平台模板+快照管理器）

**修复问题汇总：**
- 修复风格标签重复注入（同一风格不二次追加）
- 修复ELIZA标签同义词重复（同义词去重，同组只保留最优）
- 修复氛围光影重复追加（检测已存在则跳过）
- 修复关键词填入逻辑（菜单化选择填入位置）
- 修复动作→运镜不实时匹配（绑 `oninput` 事件）
- 修复多镜头批量时长无撤销（快照管理器）
- 修复移动端排版错位（600px断点响应式）
- 修复复制兼容（textarea降级方案）

**新增模块能力：**
- 场景模板库（10套预设场景：古风治愈、赛博、虐心、高燃、甜宠、悬疑、社畜、奇幻、科幻、克苏鲁）
- 同义词冲突检测（返回 `conflicts` 数组，供UI层处理）
- 用户自定义反射规则（localStorage持久化）
- 安全填入工具函数（数组操作，替代正则拼接）

---

## 版本演进路线图（V1 ~ V7）

```

V1.0 基础表单
↓
V1.1 文件上传 + 参考文本
↓
V2.0 关键词提取 + 风格标签 + 历史记录
↓
V3.0 多镜头分段系统
↓
V3.2 TextRank引擎
↓
V3.3 混合提取模式
↓
V4.0 全题材词表扩展
↓
V5.0 ELIZA反射模式
↓
V5.5 光影映射 + 平台适配器（合入V6）
↓
V6.0 搜索增强单文件版
↓
V7.0 模块化架构（1+4文件）

```

---

## V8.0 · 功能完整版

**核心目标：** V7 模块化架构下，所有已实现但未暴露的能力全部接通，同时修复 12 个已知问题。

**新增/接通功能：**
- 场景模板入口（场景字段旁按钮，弹出 10 套预设模板一键填充）
- 快照撤销（批量设置时长前自动保存快照，支持一键撤销）
- 多镜头复制按钮（每个镜头面板新增复制按钮，在下方插入副本）
- 拖拽排序（镜头卡片支持 HTML5 Drag & Drop 拖拽重排，保留上下移动按钮）
- 切换模式数据保护（切到单镜头时保存多镜头快照，切回时询问恢复）
- 自定义负面词（负面清单底部新增输入框，添加自定义词并持久化到 localStorage）
- 多镜头分段复制（每个镜头输出块独立，提供“复制本段”按钮）
- 覆盖字段视觉标记（有覆盖值的镜头卡片橙色边框+标题旁橙色圆点）
- 移动端菜单边界检测（右侧空间不足时自动向左弹出菜单）
- `400px` 响应式补全（新增 400px 断点，处理批量行、负面清单、镜头网格等小屏布局）
- 模块加载错误处理（待后续补充，当前架构主要依赖浏览器控制台报错）
- 短词条正则边界（1-3字实体词自动添加前后非汉字断言，防止误匹配）
- 反射规则优先级（每条规则增加 `priority` 字段，每个句子只保留最高优 1-2 条结果）

**修复问题汇总（对应 V7 问题）：**
- V7-1 场景模板库已实现但无入口 → 新增模板选择面板
- V7-2 快照管理器已实现但未接入 → 批量时长前自动保存快照，提供撤销按钮
- V7-3 多镜头缺少拖拽排序和复制 → 新增拖拽排序和复制按钮
- V7-4 单/多镜头切换无数据保护 → 切换时自动保存/提示恢复快照
- V7-5 关键词菜单移动端溢出 → 边界检测，右方空间不足时向左弹出
- V7-6 负面提示词不支持自定义 → 新增自定义负面词功能并持久化
- V7-7 多镜头输出无法分段复制 → 分段复制按钮
- V7-8 短词条正则误伤 → `dictionaries.js` 自动为短词添加边界断言
- V7-9 模块加载无错误处理 → 暂未做显式降级 UI，控制台可见报错
- V7-10 覆盖字段无视觉标记 → 橙色边框+圆点标记
- V7-11 移动端响应式不全 → 400px 断点补全
- V7-12 反射规则优先级冲突 → 规则增加优先级，输出按优先级截断

---

## V8.3 · 叙事文本兼容 + UI 修复 + 大纲对齐

**核心目标：** V8.0 功能完整版的基础上，修复关键词菜单遮挡、词典对小说文本兼容不足、反射规则碎片化等问题，并与 v0.03 设计大纲完成首次对齐。

**新增功能：**
- 关键词菜单升级为全局浮层（从标签内嵌 `<span>` 改为动态创建 `fixed` 定位菜单，追加到 `body`，`z-index` 提升至顶层，彻底解决被“提取模式”等区块遮挡的问题）
- 菜单边缘检测（优先下方展开，空间不足时自动向上翻转；右边界不足时向左偏移，适配移动端窄屏）
- 全局点击关闭（点击菜单外任意区域自动关闭，移除旧菜单）

**词典模块（`dictionaries.js`）扩容：**
- 小说/叙事向词条大幅扩充（新增明清官场、人物身份、动作姿态、四字成语画面词等，词条总量翻倍）
- 词边界断言策略修正（2字词保留边界防止误匹配，3字及以上短语取消边界，允许“摇头道”“含泪的眼眸”等叙事短语被正确提取）
- 停用词表扩充（新增“笑道”“说道”“骂道”“俺爹”等对话高频虚词，减少无效提取）
- 标准提取上限从 15 放宽至 20

**反射规则引擎（`eliza-rules.js`）重构：**
- 禁用白描“与”拼接规则（旧规则导致“张安与息道”“他很清楚与还有许多”等大量异常结果）
- 反射结果过滤增强（自动排除包含“与”且长度异常的拼接短语）
- 白描规则 `conditions` 收紧（短句判断阈值提升，避免误触发）
- 句子保留策略修正（长句保留最多 2 条反射，短句保留 1 条，减少冗余）
- 反射结果上限从 15 放宽至 20

**叙事反射规则扩充（V8.3 新增）：**
- 人物出场类（检测“走进/踏入/闯入/闪现”等动词结合人物身份，反射出“黑衣僧人步入的瞬间”“不速之客的到来”）
- 冲突对峙类（检测“对峙/怒视/拔剑/冷笑”等对抗信号，反射出“紧张的对峙瞬间”“拔剑的刹那——寒光出鞘”）
- 情绪转折类（检测“醍醐灌顶/恍然大悟/脸色骤变/猛然想起”等转折信号，反射出对应叙事短语）
- 紧张氛围类（检测“落针可闻/鸦雀无声/山雨欲来/老鼠见了猫”等氛围描述，反射出对应氛围短语）

**风格预设模块（`style-presets.js`）对齐 v0.03 大纲：**
- 新增风格标签：🏯 古装正剧、📜 文人画境
- 新增氛围映射：肃穆、对峙、寂静（补充历史/朝堂/江湖叙事高频氛围）
- 清理画质堆砌表达（伦勃朗式侧光 → 半脸隐入阴影，过曝1档 → 画面微微泛白，35mm胶片质感 → 胶片质感）
- 扩充古代动作运镜映射（新增 拱手、作揖、拔刀、拂袖、跪拜、策马、顿悟）
- 新增场景模板：🏯 朝堂对峙、⚔️ 江湖夜雨、📜 文人独坐

**修复问题汇总（V8.0 → V8.3）：**
- V8-13 关键词菜单被“提取模式”区块遮挡 → 全局 `fixed` 浮层，`z-index: 99999`
- V8-14 反射模式产生大量“X与Y”拼接 → 禁用白描规则，过滤含“与”的异常反射
- V8-15 词典对小说文本覆盖率低 → 词库翻倍，边界策略修正
- V8-16 反射结果碎片化、无叙事画面感 → 扩充叙事反射规则（出场/对峙/转折/氛围）
- V8-17 风格预设与 v0.03 大纲未对齐 → 新增古装/文人风格，清理禁用表达

**附录：v0.03 对齐路线图**

v0.03 中以下能力已确认排期至后续版本：

| 能力 | 排期版本 |
|------|----------|
| 角色/画风守恒专项规则 | V13（consistency-locker.js） |
| 平台适配的镜头位置策略 | V9+（已在 `platform-adapters.js` 预留扩展点） |
| 防错机制的正向约束（对比引导、数值锁定） | V10+（生成逻辑层实现） |
| 短视频时长建议表透出到 UI | V12（storyboard-generator.js） |
| 复合运镜模板库 | V12（`style-presets.js` 新增 `compoundCameraTemplates`） |
| 动作链时序解析 | V12（action-chain-parser.js） |
| 情感语境感知（暧昧/试探/嫉妒/在意） | V11（第16类上下文累积权重扩充） |
| 辩证反射——矛盾统一 | V10（dialectical-reflector.js） |
| 语义制导（词向量+分词器+Worker） | V9（基础架构升级） |

---

## V8.4 · 反射风格控制 + 自定义反射规则 UI

**核心目标：** 补全反射模式的可控性，将白描/文丑权衡交给用户，并补齐 V8.3 遗留的“用户自定义反射规则无前端入口”问题。

**新增功能：**

- 🎚️ **反射风格滑动条（白描 ←→ 文丑）**
  - 绿→橙→红渐变色轨道，拖动时实时刷新反射结果
  - 白描端（<25）：自动去修饰，保留动作本身
  - 文丑端（>75）：自动追加“刹那·流转·氤氲”等画面感修饰
  - 反射标签随风格变色（绿/金/红），一眼识别当前倾向
  - `applyReflectStyle()` 后处理函数，零侵入现有规则引擎

- 🔧 **自定义反射规则 UI**
  - 输入正则模式 + 反射结果 + 优先级，一键添加
  - 规则列表持久化到 localStorage，支持单条删除
  - 与内置规则同优先级队列混合执行
  - 解决了 V8.3 遗留问题“`addUserRule` 已就绪但无前端入口”

**模块微调：**
- `eliza-rules.js` 新增暴露 `saveUserRules` 方法，供外部删除/修改规则后持久化

---

## V8.5 · UI 模块化拆分（零功能变更）

**版本定位：** 纯工程重构，应对 V8.4 主文件膨胀至 ~850 行的维护瓶颈。不新增任何功能，不修改任何业务逻辑。

**文件结构变化：**

```

V8.4 结构                    →    V8.5 结构
prompt-tool-v8.4.html             prompt-tool-v8.5.html  (~80行，仅骨架+加载顺序)
modules/                               modules/
dictionaries.js                        dictionaries.js
eliza-rules.js                         eliza-rules.js  (v8.4版，含saveUserRules)
style-presets.js                       style-presets.js
platform-adapters.js                   platform-adapters.js
ui/
styles.css               (全部样式)
ui-utils.js              (toggleSection/escapeHtml/copy等)
mode-manager.js          (模式切换/多镜头/拖拽排序)
extract-manager.js       (提取/反射/菜单/滑动条)
form-bindings.js         (风格标签/场景模板/清空)
negative-panel.js        (负面提示词面板)
output-panel.js          (输出/复制/历史)
init.js                  (加载检测+顺序初始化+骨架渲染)

```

**核心原则：**
- 零功能变更，所有交互行为与 V8.4 完全一致
- 全部模块保持 `window.xxx` 挂载，`file://` 直接打开兼容
- 外部 CSS + 最小内联样式兜底
- `init.js` 含路径兼容检测：文件被单独移动时显示明确修复指引
- 所有脚本使用 `defer` 并行加载，不阻塞渲染

**影响范围：**
- 引擎模块（`modules/`）代码零改动
- 后续功能开发只需修改对应 UI 组件文件，主文件不再膨胀
- 为 V9 更复杂的功能模块化打下基础

---

## V8.7 · 反射变体 + 基础上下文 + 省略号/乱码规则 + 伏笔识别

**版本定位：** 在 V8.5 模块化架构基础上，一次综合性的功能增强版本。引入变体池消除“模板疲劳”；实现句间因果/时序上下文规则；新增省略号无语和乱码主题检测；新增伏笔识别引擎；扩充克苏鲁/赛博朋克/系统错误等词库。为后续 V9 架构升级奠定完整的基础层能力。

**核心目标：**
1. 同一规则多次触发不再永远返回相同字符串
2. 相邻句的因果/时序关系能被合并为复合反射
3. 识别省略号/句号堆叠的无语场景，按主题（克苏鲁/赛博朋克/恐怖/系统错误）产出专属反射
4. 检测乱码对话行，按主题判定精神错乱/数据损坏/灵异干扰
5. 通过存原句 + 相似度对比，识别复读机式伏笔和长短期伏笔

**新增功能：**

- **反射变体池系统**
  - 高优先级规则（眼泪/拔剑/对峙/微笑/寂静等）内置 3~6 个变体
  - 同一规则每次触发优先使用未用过的变体，全部轮过后重置
  - 白描/文丑滑动条自动适配对应半区的变体
  - 变体标记存在于会话中（刷新后重置），完全兼容旧版无池规则

- **句间因果/时序上下文规则**
  - 新增 5 条基础上下文规则：推门→众人目光、回眸→对视、跪下→俯视、拔剑→后退、寂静→打破
  - 上下文反射结果以特殊样式标签显示（渐变背景 + 虚线边框 + `⟷` 标记）

- **省略号无语规则**
  - 两人相对无言（`A: ... B: ...`）、多人沉默、六句号无语、中文句号无语、纯省略号对话、沉默后开口、说完只剩省略号
  - 主题检测：若全文含克苏鲁关键词→古神低语/疯狂失语；含赛博朋克→信号丢失/数据包全丢；含恐怖→不敢开口/恐惧哑然
  - 纯省略号/句号且无法匹配前后词时静默跳过

- **乱码无语规则**
  - 克苏鲁精神错乱：高比例非中文字符 → san值归零/语言系统崩溃/异界低语
  - 赛博朋克数据损坏：符号堆叠（`#$%^&@!`） → 数据包损坏/脑机接口入侵/神经链路干扰
  - 系统错误乱码：ERROR前缀/十六进制/二进制 → 未捕获异常/堆栈溢出
  - 恐怖灵异方块：Unicode方块（`████`） → 信息被污染/记录被抹去
  - 每种主题均有变体池，同一主题多次触发不重复
  - 无法识别的乱码类型静默跳过

- **伏笔识别引擎（独立模块）**
  - 存原句 + Jaccard 相似度对比机制
  - 入库检测：引号内词汇、过度修饰、精确数字/日期、视角切换聚焦、条件暗示句、信息差揭露、明示伏笔
  - 再次遇到同一词时，与原句做相似度对比
  - 高相似度（>0.6） + 距离>3000字 → 长期伏笔（复读机式伏笔）
  - 中相似度（0.3~0.6） → 疑似伏笔
  - 氛围剧变（平静→恐惧/紧张/黑暗）→ 戏剧张力升级标记
  - 手动添加伏笔接口（作者本人标注已知伏笔）
  - 词库上限可调（10/30/50/100/200），超限自动踢出低异常分项
  - 伏笔控制区 UI：开关、手动添加、上限选择、重置、词库列表折叠面板
  - 伏笔标签视觉区分：紫色＝活跃、红色＝长期、黄色＝短期、绿色＝手动

- **ELIZA 引擎新增规则**
  - `contextRules` 新增 10 条省略号规则 + 12 条乱码规则 + 5 条伏笔前置信号规则
  - 新增 `detectTheme()` 方法：检测全文是否含克苏鲁/赛博朋克/恐怖/系统错误关键词
  - 新增 `poolUsage` 状态管理：按规则索引追踪变体使用记录

- **词典扩充**
  - 克苏鲁/恐怖/灵异词条（古神、旧日支配者、触手、理智、san值、诅咒、闹鬼……）
  - 赛博朋克/科幻词条（义体、脑机、黑客、全息、数据流、神经元……）
  - 系统错误/技术故障词条（崩溃、宕机、堆栈、溢出、死锁、校验……）
  - 心理/精神词条（焦虑、抑郁、解离、创伤、共情……）
  - 职场/都市生活词条（裁员、KPI、OKR、报销、审批……）

**模块变更：**

| 文件 | 变更类型 | 说明 |
|------|----------|------|
| `modules/eliza-rules.js` | 重构 | 新增变体池逻辑(`pickFromPool`)、`detectTheme()`；`contextRules` 从 5 条扩至 32 条；`extract()` 支持全文参数传递 |
| `modules/foreshadow-engine.js` | 新增 | 伏笔识别引擎：入库检测 + 原句相似度对比 + 长短期判定 + 手动添加 |
| `modules/dictionaries.js` | 扩充 | 新增克苏鲁/赛博朋克/系统错误/心理/职场五大类词条 |
| `ui/extract-manager.js` | 功能新增 | 新增伏笔开关、手动添加、上限设置、重置、面板渲染、初始化 |
| `ui/init.js` | UI 新增 | 新增伏笔控制区骨架；初始化时配置伏笔引擎 |
| `ui/styles.css` | 样式新增 | 新增伏笔标签(`.foreshadow-tag`)和面板(`.foreshadow-pool-list`)全套样式 |
| `prompt-tool-v8.7.html` | 微调 | 新增 `foreshadow-engine.js` 加载标签 |
| `tests/engine-test-v8.7.html` | 测试新增 | 新增 6 条伏笔检测用例；加载 `foreshadow-engine.js` |

**回退机制：**
- 变体池和上下文规则为基础层永久能力，不受任何扩展层模块（语义制导、辩证反射等）的影响
- `ForeshadowEngine` 加载失败 → `ExtractManager` 跳过伏笔扫描，反射和提取功能不受影响
- 伏笔引擎被用户手动关闭 → 跳过扫描，词库不累积，UI 按钮显示“关”状态
- 伏笔词库超上限 → 自动踢出 `anomalyScore` 最低的词条

**已知限制：**
- 上下文规则仅检查相邻句对，不处理跨多句逻辑
- 变体池内置于代码中，用户不可自定义变体内容
- 伏笔引擎临时词库无持久化（刷新即丢失），仅检测特征信号，不做语义因果推理
- 省略号/乱码规则依赖主题检测，纯无主题文本的乱码行静默跳过

---

## V8.7.7 · 架构修复与工具链升级

**版本定位：** V8.7 的修复与完善版本。修复三个致命崩溃级 Bug、完成伏笔引擎模块化拆分、引入引擎配置系统与模块完整性检测、升级测试工具并拆分测试用例库、改装错误页为分流中心。

**架构变更：**

- **伏笔引擎拆分为 6 个独立子模块**
  - `modules/foreshadow-engine/token-utils.js` — 分词、Jaccard、句子提取、氛围检测
  - `modules/foreshadow-engine/anomaly-scorer.js` — 异常度评分、入库判定
  - `modules/foreshadow-engine/noise-filter.js` — 噪音词过滤（含例外列表，避免误杀“王座”“将军”“黎明”等核心词汇）
  - `modules/foreshadow-engine/protagonist-detector.js` — 主角人名推定
  - `modules/foreshadow-engine/pool-manager.js` — 伏笔词库管理
  - `modules/foreshadow-engine/foreshadow-engine.js` — 索引组装文件
  - 命名空间隔离：挂载 `window.__ForeshadowXxx`，避免与 ELIZA 的 `pool-manager.js` 冲突

- **新增模块**
  - `modules/engine-config.js` — 引擎配置管理器，支持引擎级（反射/伏笔）和面板级（风格标签/场景模板/负面提示词/历史记录）分层开关，配置持久化到 localStorage
  - `tests/lib/module-checker.js` — 模块挂载点完整性检测器
  - `tests/lib/file-scanner.js` — 文件加载完整性扫描器
  - `tests/scenarios/` — 测试用例库（按类别拆分为 pool/context/ellipsis/gibberish/foreshadow/standard/refresh 共 7 个文件）

- **测试工具升级**
  - 测试用例从 HTML 内联拆分为独立场景文件，新增用例只需添加文件
  - 支持 `?from=error&missing=xxx` 反向适配：从主入口错误页跳转时自动运行模块检测并高亮缺失项
  - 页面顶部同时显示模块挂载检测和文件完整性检测两个横幅

- **错误页分流改造**
  - 模块加载失败时不再只显示静态错误信息，新增「进入检测工具」和「进入精简版」两个分流按钮
  - 精简版（`tests/eliza-ie11.html`）支持 `?mode=fallback` 降级提示和 `?auto=ie` IE 自动跳转标记
  - 主入口 → 检测工具 → 精简版 三者互相连通，用户不会被卡住

**修复问题：**

- 🔴 `eliza-rules.js` 中 `const` 变量 `candidates` 被重新赋值导致运行时崩溃 → 改为 `let`
- 🔴 用户自定义规则加载时 `new Function` 语法错误导致页面初始化中断 → 新增 `parseFunctionString` 安全解析，降级返回空函数
- 🔴 `theme-detector.js` 正则 `g` 标志污染 `lastIndex` 导致主题检测结果随机出错 → 全部去掉 `g` 标志
- 🟠 伏笔引擎噪音过滤误杀核心词汇（王座、将军、道士、黎明等）→ 增加 `NOISE_EXCEPTIONS` 例外列表，收紧姓氏过滤条件
- 🟠 `context-rules.js` 省略号规则不支持中文省略号 `……` → 正则全部兼容 `…{2,}`
- 🟠 `output-panel.js` 多镜头复制按钮换行符截断 HTML 属性 → 新增 `safeAttr` 函数处理特殊字符
- 🟠 `output-panel.js` 多镜头验证失败不阻止生成 → 改用 `for...of` + `return` 替代 `forEach`
- 🟡 伏笔引擎同一词多次出现时位置计算只取第一次 → 改为 `while` 循环查找所有出现位置
- 🟡 伏笔引擎 `totalCharsRead` 在重复扫描同一文本时异常累加（祖传 bug）→ `extract-manager.js` 新增漂移检测日志

**新增功能：**
- 引擎配置系统：引擎/面板分层开关，用户可按需组合功能模块
- 模块完整性检测：检测工具初始化时自动对比预期清单，缺失/新增以红绿色横幅提醒
- 动态刷新接口：`ElizaRules.refreshExtract(text, maxRefreshes)` 多次提取并合并结果
- 细化武器拔剑规则：区分利器/钝器重兵/远程特殊武器，各自产出不同变体
- 扩展动作场景规则：新增震惊、声音消失、风尘仆仆、江山社稷、末日降临等反射
- 鸦雀无声变体：新增鸦雀无声+句号（沉默加深）、鸦雀无声+正常开口（打破沉默）
- 省略号正常说话防护：检测正常对话内容时不触发无语反射
- IE11 精简版增加功能开关：反射/伏笔/乱码三个独立开关，同步 v8.7.7 规则
- 主入口增加引擎设置面板：引擎总控开关 + 面板显隐设置，折叠在参考文本区域上方
- 伏笔面板联动刷新：每次提取后自动同步伏笔面板

**模块影响范围：**

| 文件 | 变更类型 | 说明 |
|------|----------|------|
| `modules/eliza-rules.js` | 修复 | 修复 const 重赋值崩溃、用户规则加载语法错误；新增 `refreshExtract` |
| `modules/eliza-engine/theme-detector.js` | 修复 | 去除所有正则 `g` 标志 |
| `modules/eliza-engine/builtin-rules.js` | 扩展 | 细化武器规则、新增震惊/声音消失/风尘仆仆/江山社稷等反射 |
| `modules/eliza-engine/context-rules.js` | 修复+扩展 | 支持中文省略号；新增鸦雀无声变体/正常说话防护 |
| `modules/foreshadow-engine/` | 重构 | 拆分为 6 个子模块 |
| `modules/dictionaries.js` | 修复+扩展 | 新增系统错误/技术故障词汇；同时挂载两个名字兼容新旧引用 |
| `modules/engine-config.js` | 新增 | 引擎配置管理器 |
| `ui/extract-manager.js` | 修复+扩展 | 集成引擎配置条件跳过、面板显隐同步、伏笔面板联动刷新、字数漂移检测 |
| `ui/init.js` | 修复+扩展 | 错误页分流按钮；引擎设置面板渲染 |
| `ui/output-panel.js` | 修复 | 换行符截断/验证不阻止生成 |
| `tests/engine-test-v8.7.html` | 重构 | 测试用例外置；支持反向适配；双重完整性检测 |
| `tests/eliza-ie11.html` | 扩展 | 功能开关；降级提示；同步 v8.7.7 规则 |
| `tests/lib/module-checker.js` | 新增 | 模块挂载点检测器 |
| `tests/lib/file-scanner.js` | 新增 | 文件完整性扫描器 |
| `tests/scenarios/*.js` | 新增 | 按类别拆分的 7 个测试用例库 |

**已知限制：**
- 伏笔引擎临时词库刷新后丢失，无持久化
- 上下文规则仅限相邻句对，不处理长距离依赖
- 变体池内置于代码中，用户不可自定义变体内容
- 测试用例未覆盖 `engine-config.js` 和 `flow-inspector.js`

**版本演进标记：**

```

V8.5 模块化拆分
↓
V8.7 反射变体 + 基础上下文 + 省略号/乱码规则 + 伏笔识别
↓
V8.7.7 架构修复与工具链升级 ★ (V8.7.x 最终)
↓
V9 分词器 + 语义雷达 + Worker + 分册词域推理微核（架构升级）

```

---

## V8.8 · 主角身份档案与三阶段混合管线

**版本定位：** V8.7.7 的结构性升级。伏笔引擎从“找名字”升级为“建立完整主角身份档案”，并通过上下文注入器与 ELIZA 反射引擎融合，实现定向叙事转译。新增过渡引擎将被两个引擎跳过的过渡性叙述反向回收，填充到输出结构中。新增综合模式作为第三种提取模式。

**核心变化：**
1. 伏笔引擎的主角检测从“找名字”升级为“建立完整身份档案”——包含姓名、种族、等级、获取物、进化链五个维度
2. 伏笔引擎的产出不再仅供内部使用，通过上下文注入器与 ELIZA 反射引擎融合，实现定向叙事转译
3. 新增过渡引擎，将被两个引擎跳过的过渡性叙述反向回收，填充到输出结构中
4. 新增可开启的种族/物种检测通道，处理非人主角、物品主角、进化文等传统检测器无法覆盖的小说类型

**新增功能**

### 主角身份档案系统（5 个新增模块）

- **物种身份检测器（`species-detector.js`）**
  - 双层检测机制：预定义词典（120 词条）+ 正则骨架（3 条）
  - 五种提取规则：系统面板格式（置信度 2.0）、种族/形态声明（1.0）、归属声明（0.5）、命名行为（0.6）、进化目标（0.5）、格式特征兜底（0.7）
  - 复合词保护：“暗影暴龙”整体保留，不拆分为“暴龙”
  - 去噪加权：单字动物名 ×0.2，双字动物名 ×0.5，有进化/命名/系统面板则恢复权重
  - 进化链追踪：前缀/后缀重叠度 >0.5 合并为同一进化链

- **等级后缀字频检测器（`power-level-detector.js`）**
  - 后缀字频统计：自动发现任何世界观的等级体系（不限于修仙/玄幻）
  - 前缀多样性验证：重复率 <30% 确认为等级系统
  - 多位置感知：检测开头字高频前缀（如 Lv）
  - 子等级拆分：“筑基期初期” → 主等级“筑基期” + 子等级“初期”
  - 内置词表交叉验证：命中内置词表置信度 +0.3

- **获取物追踪器（`acquisition-tracker.js`）**
  - 20 个触发动词检测（获得/继承/觉醒/绑定/炼化/吞噬...）
  - 三层归属推断：近距离身份标签匹配 → 段落主语延续 → 默认归属
  - 五级物品权重：S 级（系统/金手指）10 分 → D 级（一般物品）1 分
  - 按归属者汇总获取物总权重

- **身份合并引擎（`identity-merger.js`）**
  - 合并输入：人名候选人 + 物种身份标签 + 等级信息 + 获取物统计 + 进化链
  - 位置接近度合并：200 字内视为同一实体
  - 五维度填充：姓名、种族、等级、获取物、性别
  - 综合置信度计算：人名权重 ×2 + 物种置信度 + 获取物权重 ×0.1 + 进化链加成 + 等级命中

- **上下文注入器（`context-bridge.js`）**
  - 将主角身份档案格式化为 `[主角:xxx][种族:xxx][等级:xxx]` 声明
  - 注入到原始文本头部，供 ELIZA 反射引擎消费
  - 降级逻辑：伏笔引擎未开启或无主角时自动跳过

### ELIZA 定向反射（`eliza-rules.js` 修改）

- 新增 `extractWithContext(text, protagonistContext)` 方法
- 新增 5 条定向反射规则：
  - 规则 1：等级突破 → “噬魂蚁突破金丹期的瞬间”
  - 规则 2：种族凝视 → “暗影蚁族的凝视”
  - 规则 3：进化蜕变 → “噬魂蚁蜕变为噬魂蚁皇的刹那”
  - 规则 4：说话人 → “噬魂蚁开口的瞬间”
  - 规则 5：获取物揭示 → “噬魂蚁获得系统的瞬间——面板浮现”
- 定向反射与通用反射共用优先级排序和去重管道

### 过渡引擎（`transition-engine.js`）

- 收集伏笔引擎和反射引擎的屏蔽词
- 定位核心短语之间的空缺区域
- 反向提取原文本中空缺区域的过渡性内容
- 画面暗示过滤：保留包含具象名词 + 动作动词的句子，丢弃纯功能性过渡
- 交替插入输出结构：核心 → 过渡 → 核心 → 过渡
- 降级逻辑：过渡引擎未开启时一二阶段正常运行

### 综合模式（`extract-manager.js` 修改）

- 新增“综合模式”作为第三种提取模式（标准 / 反射 / 综合）
- 三阶段管线调度：
  - 阶段 1：运行伏笔引擎，产出 `protagonistProfile`
  - 阶段 2：调用 `contextBridge.injectContext()` + `elizaRules.extractWithContext()`
  - 阶段 3：调用 `transitionEngine`，回收屏蔽词，填充输出结构
- 综合模式按钮：🔀 综合

### 引擎配置新增 5 个子开关（`engine-config.js` 修改）

- `speciesDetection`（默认关闭）
- `powerLevelDetection`（默认开启）
- `acquisitionTracking`（默认开启）
- `contextInjection`（默认开启）
- `transitionEngine`（默认开启）

### 主角检测器升级（`protagonist-detector.js` 修改）

- 保留原有 `detect()` 和 `detectAll()` 人名检测
- 新增 `detectIdentityProfiles(text)`：输出完整 `protagonistProfile` 数组
- 内部集成：`species-detector` + `power-level-detector` + `acquisition-tracker` + `identity-merger`
- 降级路径：merger 不可用时手动构建简易档案

### 主角追踪器升级（`protagonist-tracker.js` 修改）

- 保留原有 `recordScan()` 方法（向后兼容）
- 新增 `recordScanProfiles(profiles, totalCharsRead, text)`：接收 `protagonistProfile` 数组
- 新增 `cumulativeProfiles` 存储维度历史（物种/等级/获取物/进化链）
- 长期主角判定扩展：获取物权重持续增长 → 提升判定置信度
- `getReport()` 返回中新增 `cumulativeProfiles` 字段

**文件结构变化**

| 文件 | 变更类型 | 说明 |
|------|----------|------|
| `modules/foreshadow-engine/species-detector.js` | 新增 | 物种身份检测器 |
| `modules/foreshadow-engine/power-level-detector.js` | 新增 | 等级后缀字频检测器 |
| `modules/foreshadow-engine/acquisition-tracker.js` | 新增 | 获取物追踪器 |
| `modules/foreshadow-engine/identity-merger.js` | 新增 | 身份合并引擎 |
| `modules/foreshadow-engine/context-bridge.js` | 新增 | 上下文注入器 |
| `modules/foreshadow-engine/transition-engine.js` | 新增 | 过渡引擎 |
| `modules/foreshadow-engine/protagonist-detector.js` | 修改 | 新增 `detectIdentityProfiles()` 方法 |
| `modules/foreshadow-engine/protagonist-tracker.js` | 修改 | 新增 `recordScanProfiles()` 方法，扩展维度追踪 |
| `modules/eliza-rules.js` | 修改 | 新增定向反射规则 + `extractWithContext()` 方法 |
| `ui/extract-manager.js` | 修改 | 新增综合模式（三阶段管线调度） |
| `modules/engine-config.js` | 修改 | 新增 5 个伏笔引擎子开关 |
| `prompt-tool-v8.8.html` | 修改 | 新增 6 个模块加载标签 + 综合模式按钮 |
| `ui/init.js` | 修改 | 版本号更新，新增综合模式按钮 |
| `ui/styles.css` | 修改 | 新增过渡标签样式 `.reflected-transition` |

**降级路径**
- 综合模式下，如果伏笔引擎或反射引擎未开启 → 自动降级到可用模式
- 伏笔引擎未检测到主角 → 上下文注入器跳过 → 反射引擎正常反射
- 过渡引擎未开启 → 一二阶段正常运行，无过渡填充
- `identity-merger` 不可用 → `protagonist-detector` 手动构建简易档案
- 任何新增模块加载失败 → 不影响已有标准/反射模式正常运行

**已知限制**
- 综合模式需要伏笔引擎和反射引擎同时开启
- 物种检测器默认关闭（需在引擎设置中手动开启）
- 过渡引擎的屏蔽词收集依赖两个引擎各自维护屏蔽列表
- 身份档案的跨窗口稳定性依赖于 `protagonist-tracker` 的窗口分析（至少 3 万字才触发）

---

## V8.8.1 · 多文件批量处理 + 自然排序 + 滑条中间值修复

**版本定位：** V8.8 的功能增强与体验优化版本。主要解决用户反馈的“反射滑条拉到中间感觉没变化”的问题，并大幅扩展工具的应用场景——支持批量上传多个章节文件并按小说自然顺序检测。

**新增功能：**

- **多文件章节上传与自然排序**
  - 文件选择器支持同时选择多个文件（`accept` 扩展至 `.txt/.md/.markdown/.text`）
  - 智能排序函数 `naturalSortFiles()`：支持提取“第1章”“第一章”“第10节”“第一百零八回”等章节号，将中文数字（一~十百千万）转换为数字进行比较
  - 排序后文件列表显示在界面上，每个文件带序号
  - 不改变原文件名的同时，可按章节顺序依次处理

- **顺序检测（批量处理）**
  - 新增“顺序检测（按章节）”按钮
  - 点击后依次读取每个文件内容，调用伏笔/反射引擎提取关键词
  - 实时显示进度条、当前处理第几个文件/总数
  - 中间结果区域实时追加每个文件的提取摘要（关键词列表）
  - 处理完成后给出汇总提示
  - 不影响单文件上传/粘贴的原有使用方式

- **提取管理器增加外部调用接口**
  - `ExtractManager.extractFromContent(text, fileName, callback)` 供批量处理调用
  - 不依赖 DOM 中的 `referenceText` 文本框，避免 UI 刷新冲突

- **反射风格滑条体验优化**
  - 修复 bug：原先拉杆在中间值（40-60）时输出完全无变化，用户误以为功能失效
  - 重新定义滑条区间：
    - 0-20：白描（去修饰）
    - 20-40：偏白描（保留原句，不加词）
    - 40-60：均衡（原句，不增不减，显示“⚖️ 均衡”提示）
    - 60-80：偏文丑（加少量形容词）
    - 80-100：文丑（加丰富修饰词）
  - 提示文字实时变化（🌿 白描 → 🌤️ 偏白描 → ⚖️ 均衡 → ✨ 偏文丑 → 💎 文丑）
  - 中间值用户获得明确反馈（提示文字变化 + 输出保留原句），消除“没反应”的困惑

**模块变更：**

| 文件 | 变更类型 | 说明 |
|------|----------|------|
| `ui/extract-manager.js` | 增强 | 新增 `extractFromContent` 方法；`applyReflectStyle` 细化分段逻辑；`onReflectStyleChange` 完善提示 |
| `ui/form-bindings.js` | 新增 | `handleFileUpload` 支持多文件，`naturalSortFiles` 自然排序，`startBatchProcess` 顺序检测，`readFileContent` Promise 封装 |
| `ui/init.js` | UI 修改 | 增加多文件上传区块、文件列表容器、进度条、进度文本、批量结果区域 |
| `modules/flow-inspector.js` | 保持 | 已在测试工具中增强，主工具不依赖 |

**影响范围：**
- 完全向后兼容，单文件上传/粘贴方式不受任何影响
- 批量处理为可选功能，不干扰原有工作流
- 自然排序算法覆盖常见章节命名格式（“第X章”“第X节”“第X回”“X章”“第一章”“第二十节”等），对不规范命名回退到字典序
- 滑条修复不影响已有反射产出的内容，仅改善中间值的用户体验

**已知限制：**
- 批量处理时，每个文件独立调用引擎，不跨文件累积伏笔词库（词库在每次 scan 后重置，避免跨章节污染）
- 超大文件（>10MB）可能使浏览器短暂卡顿，但未禁止，由用户自行控制
- 中文数字转换目前仅支持“一”到“十”、“百”、“千”、“万”的组合，不支持“零”“两”等变体（实际小说章节名极少出现）

**版本演进标记：**

```

V8.8 主角身份档案 + 三阶段混合管线
↓
V8.8.1 多文件批量处理 + 自然排序 + 滑条修复 ★ 当前版本
↓
V8.9 分册词域推理微核（微 NLP）（计划）
↓
V9.0 分词器 + 语义雷达 + 语言模型（架构升级）

```

---
- V8.8.1-lite · IE11 精简版升级：模块化重构，新增综合模式（手动主角设定 + 上下文注入 + 定向反射后处理），保持与主版本功能对齐，便于后续迭代。
---

*本更新日志持续更新，记录所有主要版本与重要修复。*  
*最新版本：V8.8.1（多文件批量处理 + 自然排序 + 滑条修复）*

---
## V8.8.2 · 稳定性修复与体验优化

**版本定位：** V8.8.1 的修复补丁。解决 15 个已确认问题（致命 1、严重 5、中等 6、轻微 3），涵盖文件名冲突、模块加载协调、初始化竞争、测试工具引用缺失、业务逻辑空方法等关键缺陷。

**核心修复：**

- 🔴 **文件名冲突致伏笔引擎降级**  
  - 将 `modules/foreshadow-engine/pool-manager.js` 重命名为 `foreshadow-pool-manager.js`  
  - 更新 `prompt-tool-v8.8.1.html` 和 `tests/engine-test-v8.7.html` 中的引用路径  
  - 消除下载解压后系统自动重命名导致的模块缺失

- 🟠 **模块加载监控与初始化协调**  
  - `ui/module-loader.js`：`criticalModules` 扩展至 30 个关键挂载点；修复 `getLoadedCount()` 中的 `break` 导致进度条卡死；`onComplete` 派发 `modulesReady` 事件  
  - `ui/init.js`：增加 `EngineConfig` 依赖检查；改为监听 `modulesReady` 事件再执行初始化，带 IE11 兼容降级  
  - 彻底解决模块加载进度虚假 100%、init 与 loader 竞争执行导致界面残缺等问题

- 🟡 **业务逻辑补全**  
  - `ui/extract-manager.js`：实现 7 个空方法（伏笔开关、手动添加、重置词库、渲染面板等），配合 UI 按钮正常反馈；`performExtract` 增加 `try-finally` 保证批量回调始终执行  
  - `modules/foreshadow-engine/foreshadow-engine.js`：正常与降级对象均增加 `isAvailable` 标志，调用方可明确感知引擎可用性  
  - `tests/scenarios/protagonist.js`：新建主角检测与追踪测试用例（4 条）  
  - `tests/eliza-ie11.html`：修复返回主工具链接为 `prompt-tool-v8.8.1.html`

- 🟡 **测试工具修复**  
  - `tests/engine-test-v8.7.html`：补全 `flow-inspector.js` 引用，恢复调试功能；补齐 7 个伏笔子模块引用；新增主角测试用例加载  
  - 所有调试功能（断点、单步、日志导出）现已可用

- 🟢 **健壮性增强**  
  - `modules/engine-config.js`：`load()` 与 `save()` 增加 try-catch，损坏数据时自动回退默认配置并清理  
  - `ui/mode-manager.js`：快照数据增加 `version` 字段，向前兼容  
  - 所有 JSON.parse 操作均已保护

**文件变更：**

| 文件 | 变更类型 | 说明 |
|------|----------|------|
| `modules/foreshadow-engine/pool-manager.js` | 重命名 | → `foreshadow-pool-manager.js` |
| `modules/foreshadow-engine/foreshadow-engine.js` | 修改 | 增加 `isAvailable` 标志 |
| `ui/module-loader.js` | 重写 | 全量监控、去掉 break、派发事件 |
| `ui/init.js` | 重写 | 增加依赖检查、监听 `modulesReady` 事件 |
| `ui/extract-manager.js` | 重写 | 实现空方法、try-finally、批量接口 |
| `ui/mode-manager.js` | 修改 | 快照增加 version 字段 |
| `modules/engine-config.js` | 修改 | localStorage 异常保护 |
| `prompt-tool-v8.8.1.html` | 修改 | 更新引用路径 |
| `tests/eliza-ie11.html` | 修改 | 修复返回链接 |
| `tests/engine-test-v8.7.html` | 重写 | 补全所有缺失引用、新增主角用例 |
| `tests/scenarios/protagonist.js` | 新建 | 主角检测测试用例 |

**降级与兼容性：**
- 所有修改完全向后兼容，无破坏性变更
- IE11 精简版保持与主版本功能对齐
- `modulesReady` 事件在 IE11 下自动降级为轮询检测
- 伏笔引擎不可用时，UI 给出明确视觉提示

**版本演进标记：**

------


结构化提示词生成器 · V8.9 更新报告

版本: V8.9 · HC+level 数值体系集成

日期: 2026-05-24

基础版本: V8.8.2

核心目标: 引入 HC+level 分层高精度离散坐标体系（HPDC8），实现领域置信度投票、确定性超度量排序与纯整数运算。

---

一、新增模块（2 个）

文件	职责	挂载点	
`modules/hpdc-core.js`	HPDC8 类核心：三种模式（HC_ONLY / LEVEL_ONLY / HYBRID）、纯整数比较/加法、构造器、序列化	`window.HPDC`	
`modules/domain-voter.js`	领域投票累加器：增量投票、批量添加、前 N 领域排序、调试快照	`window.DomainVoter`	

---

二、改造模块（6 个）

1. `modules/dictionaries.js` · 置信度向量
- 新增: `getDomainConfidence(word)` —— 返回 `{ 领域名: HPDC8, ... }` 映射
- 新增: `mergeDomainConfidences(wordList, options)` —— 多词领域投票累加，支持饱和/进位模式切换
- 新增: `getTopDomain(word)` —— 快速返回置信度最高的单一领域
- 保留: `getDomainForWord()`、`rawDomainMap`、`DOMAINS` 完全不变（向后兼容）
- 机制: 高频词内置 HPDC8 置信度标注，其余词按词长动态生成默认置信度，结果缓存并克隆防污染

2. `ui/extract-manager.js` · 智能字段填充
- 新增: `DOMAIN_FIELD_MAP` —— 11 个领域 → scene/subject/mood/camera 映射规则
- 新增: `fillFieldsFromTopDomains()` —— 基于领域投票前两名自动填充输出字段
- 新增: `selectBestWordForDomain()` —— 从提取词中选出对目标领域置信度最高的词
- 新增: `legacyFillFromKeywords()` —— 保留旧版一对一映射填充逻辑（降级用）
- 集成点: `performExtract()` 中 `standardExtract()` 后插入领域投票管线：词 → `getDomainConfidence` → `DomainVoter` → `getTopDomains(2)` → 智能填充
- 开关: 受 `EngineConfig.isEnabled('domainVoter')` 控制，默认关闭；未加载 HPDC 或报错时自动降级到旧逻辑

3. `modules/eliza-rules.js` · 优先级 HPDC8 化
- 迁移: 所有 `priority` 字段从 `number` 改为 `HPDC8` 实例
- 排序: `candidates.sort()` 从 `b.priority - a.priority` 改为 `HPDC8.compare(b.priority, a.priority)`
- 动态加成: `boost.cthulhuBoost` 等主题加成通过 `HPDC8.addLeveled` 合并到优先级
- 序列化: `saveUserRules()` 调用 `priority.serialize()` 存入 localStorage；`loadUserRules()` 支持新旧格式自动识别
- 降级: 无 `window.HPDC` 时自动回退到 legacy number 逻辑，暴露 `__LEGACY_PRIORITY` 标志

4. `modules/foreshadow-engine/anomaly-scorer.js` · 异常评分 HPDC8 化
- 迁移: `calculateAnomalyScore()` 的 `score` 从 `number(0~1)` 改为 `HPDC8`（仅 HC 饱和累加）
- 预定义常量: `BOOST_QUOTED`、`BOOST_OVERMODIFIED` 等 6 项加分均用 `HPDC8.fromConfidence()`
- 阈值比较: `shouldEnterPool()` 使用 `HPDC8.compare(score, THRESHOLD_ENTER) >= 0`
- 全局开关: `window.FORESHADOW_USE_HPDC` 控制是否启用（默认 true）
- 保留旧逻辑: `calculateAnomalyScoreLegacy()` 和 `shouldEnterPoolLegacy()` 完整保留，无 HPDC 或开关关闭时自动使用

5. `modules/engine-config.js` · 配置系统扩展
- 新增引擎: `domainVoter: { enabled: false }`（默认关闭，充分测试后建议开启）
- 深合并修复: `load()` 从直接替换改为 `deepMerge`，旧用户配置加载后自动补全新增字段，防止 `domainVoter` 等丢失
- 新增接口: `isSubFeatureEnabled()` / `setSubFeature()` —— 支持子功能级开关

6. `ui/init.js` · 配置持久化 UI
- 预初始化: 顶部 IIFE 从 `localStorage` 读取 `hpdcConfig`，确保 `hpdc-core.js` 加载前 `window.HPDC_CONFIG` 已就绪
- 新增面板: `initHPDCSettingsUI()` —— 渲染折叠式 HC 配置面板（小数层数 2/4/6、模式选择、饱和开关）
- 即时生效: 任何配置修改保存到 localStorage 并提示刷新页面（因 HPDC8 类 frac 长度编译期固定）
- 防重复渲染: `data-hpdc-initialized` 属性标记

---

三、入口文件调整

`prompt-tool-v8.8.1.html` → `prompt-tool-v8.9.html`
- 加载顺序调整:
  1. `hpdc-core.js`（最先，被后续 4 个模块依赖）
  2. `dictionaries.js`
  3. `domain-voter.js`（新增，依赖前两者）
  4. `engine-config.js`
  5. 其余模块保持不变
- 内联脚本: `<head>` 中插入 HPDC_CONFIG 预初始化脚本，作为 `defer` 加载前的安全网
- 标题更新: `v8.8.1` → `v8.9`

---

四、向后兼容与降级策略

场景	行为	
浏览器不支持 BigInt	`hpdc-core.js` 抛出明确错误，其余模块旧逻辑不受影响	
HPDC 未加载	`dictionaries.js` 新接口抛错但旧接口正常；`eliza-rules.js` 自动降级到 number 优先级；`anomaly-scorer.js` 使用 legacy 函数	
`domainVoter` 开关关闭	`extract-manager.js` 跳过投票逻辑，直接使用 `legacyFillFromKeywords`	
旧用户配置无 `domainVoter`	`engine-config.js` 深合并自动补全，不丢失数据	
旧 localStorage 规则 priority 为数字	`eliza-rules.js` 自动转换为 HPDC8，输出一次警告	

---

五、已知限制与待办

1. 配置变更需刷新: HPDC8 的 `frac` 数组长度在类加载时固定，运行时修改 `FRAC_LAYERS` 不生效，必须刷新页面。
2. 手动标注覆盖率低: `wordConfidenceMap` 目前仅标注 7 个高频词，需逐步扩展至全部 200+ 词条。
3. 伏笔引擎其他模块: `foreshadow-pool-manager.js` 和 `foreshadow-engine.js` 中存储的 `anomalyScore` 仍为旧格式，需后续改造以完全支持 HPDC8 比较淘汰。
4. 测试覆盖: 建议补充 `tests/scenarios/hpdc-voter.js` 测试用例，验证饱和加法、排序稳定性、跨模式比较。

---

六、文件替换清单

请按以下顺序替换/新增文件，然后刷新页面测试：

```
prompt-tool-v8.8.1.html          →  重命名/覆盖为 v8.9 版本
modules/hpdc-core.js             →  新增
modules/domain-voter.js          →  新增
modules/dictionaries.js          →  覆盖
modules/engine-config.js         →  覆盖
modules/eliza-rules.js           →  覆盖
modules/foreshadow-engine/anomaly-scorer.js  →  覆盖
ui/extract-manager.js            →  覆盖
ui/init.js                       →  覆盖
```

---


## V8.9.1 · 数据源层优先级 HPDC8 化

**变更文件：**

- `modules/eliza-engine/builtin-rules.js` — 全部内置规则 `priority` 改为 HPDC8 实例（降级自动回退 number）
- `modules/eliza-engine/context-rules.js` — 全部上下文规则 `priority` 改为 HPDC8 实例

**无功能变更**，仅与 `eliza-rules.js` 索引层类型对齐，消除运行时重复转换。

---

## V8.9.2 · 伏笔引擎 HPDC8 兼容

**变更文件：**

- `modules/foreshadow-engine/foreshadow-pool-manager.js` — 异常分淘汰/比较/上限管理兼容 HPDC8
- `modules/foreshadow-engine/foreshadow-engine.js` — 焦点词加分兼容 HPDC8 加法

-----
## V8.9.3 · 稳定性修复与检测工具完善

**版本定位：** V8.9 系列的修复补丁。基于静态分析与用户反馈，修复 4 个确认真实 Bug，完善测试工具链，确保所有核心模块稳定加载与运行。

**核心修复：**

### 一、运行时崩溃修复

1. **`hpdc-core.js` – `addLeveled` 变量名错误**  
   - 错误代码：`const y = (b.mode === HPDC8.MODE_HYBRID) ? y : normalizeToHybrid(b);`  
   - 修复为：`const y = (b.mode === HPDC8.MODE_HYBRID) ? b : normalizeToHybrid(b);`  
   - 影响：合体加法不再抛出 `ReferenceError`，反射引擎动态加成恢复正常。

2. **`foreshadow-engine.js` – 依赖缺失时未挂载空引擎**  
   - 原代码在依赖缺失分支中 `return { ... }` 但未赋值给 `window.ForeshadowEngine`，导致模块加载器永久等待。  
   - 修复：改为 `window.ForeshadowEngine = { ... }; return;`  
   - 结果：即使部分依赖加载失败，模块仍被标记为已加载，30 秒超时问题彻底解决。

### 二、功能缺陷修复

3. **`protagonist-detector.js` – 进化链构建错误**  
   - 原代码使用 `detectEvolutionTargets().map(...)` 将每个进化目标拆分为孤立链，丢失顺序关系。  
   - 修复：改用 `speciesDetector.detectEvolutionChain(text) || []`，正确输出 `["小白蛇","蛟龙","应龙"]` 链式结构。  
   - 影响：主角档案中的 `evolutionChain` 和 `chainLength` 字段现在完整可用。

4. **`transition-engine.js` – 缺失 `position` 导致过渡引擎失效**  
   - 原综合模式构建 `outputStructure` 时未携带 `position` 字段，`locateGaps` 无法正确定位空缺区域。  
   - 修复：在 `extract-manager.js` 的综合模式中为每个核心短语添加 `position`（通过 `text.indexOf(it.text)` 估算）。  
   - 结果：过渡引擎恢复功能，能正确提取空缺区域的过渡句并插入输出。

### 三、资源遗漏补全

5. **`flow-inspector.js` 加载缺失**  
   - HTML 中未包含该脚本，导致 `ExtractManager` 中的流程检测功能完全不可用。  
   - 修复：在 `prompt-tool-v8.9.html` 中所有 UI 模块之后添加 `<script src="modules/flow-inspector.js" defer></script>`。

### 四、测试工具链完善（`tests/` 目录）

6. **测试用例缺少 `mode` 字段**  
   - `standard.js` 等场景文件未显式声明 `mode`，导致运行时默认使用 `eliza` 模式，标准提取测试永远失败。  
   - 修复：为所有测试用例补全 `mode: 'standard'` / `mode: 'eliza'` 字段。

7. **`FlowInspector` 步骤重复注册**  
   - `registerInspectorSteps()` 在页面加载和调试时重复调用且未清空，导致步骤日志冗余。  
   - 修复：函数开头增加 `FI.clearSteps()`，并移除 `debugSelectedTest` 中的二次调用。

8. **主角测试模式输出数据为空**  
   - 原 `runSingleTest` 中 `mode === 'protagonist'` 分支未将检测结果存入 `result.actual`，影响调试信息展示。  
   - 修复：调用 `detectIdentityProfiles` 并将结果存入 `result.actual`。

9. **文件命名不规范**  
   - `scenarios/jbb.js` 内容实际为主角测试，文件名误导。  
   - 修复：重命名为 `protagonist.js`，并更新 `engine-test-v8.7.html` 中的引用。

10. **`extra` 模块检测被注释**  
    - `module-checker.js` 中反向检查 window 上多余模块的代码被注释，降低了完整性检查的覆盖度。  
    - 修复：取消注释并增加基于已知前缀的过滤逻辑。

**文件变更清单：**

| 文件 | 变更类型 | 说明 |
|------|----------|------|
| `modules/hpdc-core.js` | 修复 | `addLeveled` 变量名错误 |
| `modules/foreshadow-engine/foreshadow-engine.js` | 修复 | 依赖缺失时正确挂载空引擎 |
| `modules/foreshadow-engine/protagonist-detector.js` | 修复 | 进化链改用 `detectEvolutionChain` |
| `ui/extract-manager.js` | 增强 | 综合模式为核心短语添加 `position` |
| `prompt-tool-v8.9.html` | 修复 | 补全 `flow-inspector.js` 加载 |
| `tests/engine-test-v8.7.html` | 重构 | 补全所有缺失引用，修复调试逻辑 |
| `tests/scenarios/standard.js` | 修复 | 添加 `mode: 'standard'` |
| `tests/scenarios/special-effects.js` | 修复 | 添加 `mode: 'eliza'`（显式） |
| `tests/scenarios/character-emotion.js` | 修复 | 添加 `mode: 'eliza'` |
| `tests/scenarios/long-context.js` | 修复 | 添加 `mode: 'eliza'` |
| `tests/scenarios/jbb.js` | 重命名 | → `protagonist.js` |
| `tests/lib/module-checker.js` | 增强 | 恢复 `extra` 检测逻辑 |
| `tests/eliza-ie11.html` | 保持 | 确认无需引入 HPDC，维持 ES5 兼容 |

**降级与兼容性：**
- 所有修复均向后兼容，不影响已有用户配置。
- IE11 精简版保持独立，不引入 HPDC 相关依赖。
- 测试工具现在可以完整验证标准提取、反射提取、伏笔检测、主角追踪等所有核心功能。

**版本演进标记：**

```

V8.9.2 伏笔引擎 HPDC8 兼容
↓
V8.9.3 稳定性修复与检测工具完善
↓
V9.0 分词器 + 语义雷达 + Worker（架构升级）

```
---

## V8.9.4 · 判定与分类逻辑修复（判断/分类准确性专项）

**版本定位：** V8.9.3 稳定基线的专项修复补丁，聚焦"人名/种族/等级归属"与长文（小说整章）场景下的判定与分类准确性。全部修改向后兼容、无破坏性变更，回归测试 79/79 通过（dbg8 + dbg7）。

**核心修复：**

### 一、主角人名提取重写（protagonist-detector.js）
- 弃用旧碎片段落扫描，改用全局 `NAME_RE`（姓氏 + 1~2 个汉字）+ `GIVEN_STOP` 废弃词表 + `maskCompoundTerms`（屏蔽"金刚不"等复合词，避免从"金刚不坏"误出"金刚不"）+ `precededByCategory`/`CATEGORY_WORDS`（屏蔽"轩辕剑"等武器前缀前的姓氏）+ 堆收集。
- 产出零误报：`[林惊羽, 苏茹, 李沧海, 张三]`，不再混入"金刚不/轩辕剑"等。
- 复姓第二字防护（`TWO_CHAR_SURNAME`）："司马"不再误拆出"马青衫"。
- 合并级前缀消歧：跨源碎片（"司马青"⊂"司马青衫"、"林惊"⊂"林惊羽"）合并时消歧。

### 二、种族/等级误归属修复（identity-merger.js + species-detector.js）
- `species-detector.scan` 候选人新增 `position` 字段。
- `identity-merger` 新增 `clauseIndexOf` + 从句感知合并：物种/等级只在"同句"或"40 字邻域"内归属到主角，杜绝跨句张冠李戴。
- 结果：张三→龙族、李沧海→练气期、林惊羽→荒古圣体（无种族）、苏茹→金刚不坏，均正确。

### 三、信息模块生成器接入主角检测器（protagonist-detector.js）
- 新增 `generateNameCandidates`/`isSurnamePrefix`，复用 `noiseFilter`、`SURNAME`、堆拒绝，与词频池合并为共享打分，避免与 `NAME_RE` 重复或冲突。
- 内置前缀消歧（生成器内 + 合并级），消除"林惊""司马青"等子串碎片。

### 四、贪心多取防护（protagonist-detector.js）
- 新增 `trimOverGrab`：重叠回退（"李四默"←"默默" → "李四"）。
- 新增 `NAME_DENY`：拒绝"张望/张罗/陈说/王道/李代/王法"等动词碰撞（"张三望"不再误取"张望"）。

### 五、长文（整章小说）场景缺陷修复
- **异常度 NaN%**（`extract-manager.js` / `eliza-ie11.html` / `foreshadow-pool-manager.js` / `hpdc-core.js`）：根因 `getPool()` 把 HPDC8 异常分对象 JSON 序列化成了普通对象，丢失数值与方法。新增 `HPDC8.toConfidence()`，`getPool()` 序列化时转换为 `[0,1]` 数值；显示层同时兼容 number / HPDC8 两种形态。现已正确显示（如 45%）。
- **"雾中气他蹲"乱码标签**（`builtin-rules.js`）：旧代码 `'雾中'+extractAfter(s,'雾',3)` 跨句抓取产生乱码。改为 雨/雪/风/雾 四类固定意境标签（雨幕下的朦胧 / 雪原上的寂寥 / 风中的呼啸 / 雾霭中的迷蒙）。
- **"握紧口袋里那张纸的隐藏"硬编码**（`builtin-rules.js`）：原正则 `/那张纸|.../` 命中通用"那张纸"。收紧为需实际"握紧/攥紧+那张纸/纸条"动作，并拆出独立的"藏不住的线索"规则。
- **"【长期伏笔】完全相同"×10 刷屏**（`foreshadow-pool-manager.js`）：根因为同一文本被重复 scan 而未重置池/计数，使每条词库项跨约全文长度偏移自我重匹配。新增防护：`checkAndCompare` 中 `distance > text.length` 即判定为跨扫描伪匹配并跳过；单次扫描本就不刷屏，重扫现产出 0 条。
- **"号和线条"截断**（`context-rules.js`）：过度修饰规则中间距 `[\u4e00-\u9fff]{0,2}` 贪婪吞掉"的符"（"奇怪的符号和线条"→"号和线条"）。收紧为可选连词 `(?:的|地|得|之)?`，现正确提取"符号和线条"。

**文件变更清单：**

| 文件 | 变更类型 | 说明 |
|------|----------|------|
| `modules/foreshadow-engine/protagonist-detector.js` | 重写+修复 | 人名提取重写、从句感知合并接入、信息模块生成器、贪心多取防护 |
| `modules/foreshadow-engine/identity-merger.js` | 修复 | `clauseIndexOf` + 从句/邻域感知的物种/等级归属 |
| `modules/foreshadow-engine/species-detector.js` | 修改 | 候选人新增 `position` |
| `modules/foreshadow-engine/foreshadow-pool-manager.js` | 修复 | `getPool()` HPDC8→数值；`checkAndCompare` 重扫防护 |
| `modules/hpdc-core.js` | 新增 | `HPDC8.toConfidence()` |
| `modules/eliza-engine/builtin-rules.js` | 修复 | 雨/雪/风/雾固定标签；握紧/藏不住规则收紧与拆分 |
| `modules/eliza-engine/context-rules.js` | 修复 | 过度修饰中间距收紧为连词 |
| `ui/extract-manager.js` | 修复 | 异常度显示兼容 number/HPDC8 |
| `tests/eliza-ie11.html` | 修复 | 异常度显示兼容 number/HPDC8 |

**回归与兼容性：**
- 全部修改向后兼容，无破坏性变更；HPDC8 开关关闭时自动降级。
- 回归测试：`dbg8.js` 79/79、`dbg7.js` 79/79（含 3 次冷/热运行）全部通过。
- IE11 精简版保持 ES5 兼容，异常度显示同步修复。

**版本演进标记：**

```
V8.9.3 稳定性修复与检测工具完善
↓
V8.9.4 判定与分类逻辑修复 ★ 当前版本
↓
V9.0 分词器 + 语义雷达 + Worker（架构升级）
```
